---
phase: 02-core-browse-latest-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/shell/site-shell.tsx
  - src/components/shell/primary-nav.tsx
  - src/components/shell/mobile-bottom-nav.tsx
  - src/app/search/page.tsx
  - src/app/records/page.tsx
  - src/app/status/page.tsx
autonomous: true
requirements:
  - CORE-04
must_haves:
  truths:
    - Desktop routes render a sticky top navigation with the four Phase 2 labels.
    - Mobile routes render a bottom navigation that remains visible while page content scrolls.
    - Search and Records have routable placeholder pages instead of broken navigation targets.
  artifacts:
    - src/components/shell/site-shell.tsx
    - src/components/shell/primary-nav.tsx
    - src/components/shell/mobile-bottom-nav.tsx
    - src/app/search/page.tsx
    - src/app/records/page.tsx
    - src/app/status/page.tsx
  key_links:
    - src/app/layout.tsx renders the shared site shell.
    - Shared shell navigation includes links for Latest Charts, Search, Records, and Data Status.
---

<objective>
Build the shared browse shell and routable navigation structure for Phase 2.

Purpose: Establish the mobile/desktop navigation contract before wiring chart data and page-specific interactions.
Output: Shared site shell components plus top-level browse routes for Latest Charts, Search, Records, and Data Status.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-browse-latest-charts/02-RESEARCH.md
@.planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md
@src/app/layout.tsx
@src/app/page.tsx
@src/app/globals.css
</context>

<threat_model>
<trust_boundaries>
- Browser navigation events cross into App Router route resolution.
- Shared shell renders future-route links that must not send users to broken pages.
</trust_boundaries>
<stride_register>
- threat_id: TM-02-01
  category: spoofing
  component: navigation state
  disposition: mitigate
  mitigation_plan: derive active nav state from the current pathname instead of mutable client-only flags.
- threat_id: TM-02-02
  category: denial-of-service
  component: mobile navigation layout
  disposition: mitigate
  mitigation_plan: keep the shell layout lightweight and avoid blocking fetches in the shared nav wrapper.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared Phase 2 site shell</name>
  <files>src/app/layout.tsx, src/components/shell/site-shell.tsx, src/components/shell/primary-nav.tsx, src/components/shell/mobile-bottom-nav.tsx</files>
  <read_first>AGENTS.md, src/app/layout.tsx, src/app/globals.css, .planning/phases/02-core-browse-latest-charts/02-RESEARCH.md, .planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md</read_first>
  <action>Create a shared shell component set for the browse experience. Keep `src/app/layout.tsx` as the root layout, but make its body render a new `SiteShell` wrapper around `children`. Implement a desktop sticky top nav at 44px height with the brand text `BILLBOARD` and links labeled `Latest Charts`, `Search`, `Records`, and `Data Status`. Implement a mobile bottom nav that is visible below 600px width and keeps the same four destinations, truncating labels to single-word mobile variants if necessary. Use pathname-aware active-state styling so the active route uses the dark surface treatment from the UI-SPEC and the inactive routes remain neutral. Do not introduce any component library; use Tailwind classes and the existing Space Grotesk / tabular-numeric baseline.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - src/components/shell/site-shell.tsx contains `export function SiteShell`
    - src/components/shell/primary-nav.tsx contains `Latest Charts`, `Search`, `Records`, and `Data Status`
    - src/components/shell/mobile-bottom-nav.tsx contains `usePathname` or equivalent pathname-derived active state
    - src/app/layout.tsx contains `SiteShell`
  </acceptance_criteria>
  <done>Shared shell renders from the root layout with desktop and mobile navigation contracts locked in.</done>
</task>

<task type="auto">
  <name>Task 2: Add routable placeholder browse pages for unfinished sections</name>
  <files>src/app/search/page.tsx, src/app/records/page.tsx, src/app/status/page.tsx</files>
  <read_first>src/app/page.tsx, .planning/ROADMAP.md, .planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md, BillboardStats.html</read_first>
  <action>Create `src/app/search/page.tsx` and `src/app/records/page.tsx` as lightweight placeholder routes that preserve the shell contract without claiming the Phase 4 features are done. Use terse copy such as `Available in Phase 4` plus one sentence that points users back to Latest Charts. Create `src/app/status/page.tsx` as a Phase 2 placeholder route with a `Data Status` heading and skeleton structure that Plan 03 will replace with live content; do not implement database reads here yet. All three pages must use the compact section-header rhythm from the UI-SPEC and must render successfully under the shared shell.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/app/search/page.tsx contains `Available in Phase 4`
    - src/app/records/page.tsx contains `Available in Phase 4`
    - src/app/status/page.tsx contains `Data Status`
    - src/app/status/page.tsx does not contain `fetch(`
  </acceptance_criteria>
  <done>All shell destinations are routable, and only the unfinished pages remain explicit placeholders.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] Search and Records are placeholder routes, not broken links
</verification>

<success_criteria>

- All tasks completed
- Desktop top nav is sticky and mobile bottom nav exists
- Latest Charts, Search, Records, and Data Status all have routable destinations
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-browse-latest-charts/02-01-SUMMARY.md`
</output>
