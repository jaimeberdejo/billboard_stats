---
phase: 02-core-browse-latest-charts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/charts.ts
  - src/lib/data-status.ts
  - src/app/api/charts/route.ts
  - src/app/api/data-status/route.ts
autonomous: true
requirements:
  - BROWSE-03
  - BROWSE-05
must_haves:
  truths:
    - The app exposes an API for latest chart rows plus available chart weeks.
    - The app exposes an API for data status counts and latest chart dates.
    - Invalid chart query input is rejected before any database query runs.
  artifacts:
    - src/lib/charts.ts
    - src/lib/data-status.ts
    - src/app/api/charts/route.ts
    - src/app/api/data-status/route.ts
  key_links:
    - route handlers call shared lib helpers instead of embedding SQL inline.
    - chart route returns both entries and availableDates for the requested chart type.
---

<objective>
Translate the existing Python chart and data-status services into Next.js route-safe TypeScript helpers and APIs.

Purpose: Give the Phase 2 UI a stable backend contract for chart switching, week selection, and freshness/status indicators.
Output: Chart and data-status lib helpers plus `/api/charts` and `/api/data-status` route handlers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-browse-latest-charts/02-RESEARCH.md
@src/lib/db.ts
@src/app/api/health/route.ts
@billboard_stats/services/chart_service.py
@billboard_stats/services/data_status_service.py
</context>

<threat_model>
<trust_boundaries>
- Browser query parameters cross into Next.js route handlers.
- Route handlers cross into Neon PostgreSQL through `src/lib/db.ts`.
</trust_boundaries>
<stride_register>
- threat_id: TM-02-03
  category: tampering
  component: chart API query parameters
  disposition: mitigate
  mitigation_plan: allow only `hot-100` and `billboard-200`; validate `date` as an ISO date string before sending it to SQL helpers.
- threat_id: TM-02-04
  category: information-disclosure
  component: API error responses
  disposition: mitigate
  mitigation_plan: return concise error JSON for invalid input or DB failure without exposing raw SQL or stack traces.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Port the weekly chart snapshot queries into TypeScript helpers</name>
  <files>src/lib/charts.ts</files>
  <read_first>AGENTS.md, src/lib/db.ts, billboard_stats/services/chart_service.py, billboard_stats/models/schemas.py, .planning/phases/02-core-browse-latest-charts/02-RESEARCH.md</read_first>
  <action>Create `src/lib/charts.ts` with typed helpers that mirror the Python `get_weekly_chart()` and `get_available_dates()` behavior. Support only `hot-100` and `billboard-200`. Reuse the same selected fields (`rank`, `title`, `artist_credit`, `image_url`, `peak_pos`, `last_pos`, `weeks_on_chart`, `is_new`, `song_id` or `album_id`) and order rows by `rank`. Export a shared chart-entry type and a helper that returns `{ entries, availableDates, latestDate }` for a requested chart plus selected date, using the newest available date as the fallback when the request does not specify one.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - src/lib/charts.ts contains `hot-100`
    - src/lib/charts.ts contains `billboard-200`
    - src/lib/charts.ts contains `availableDates`
    - src/lib/charts.ts contains `latestDate`
  </acceptance_criteria>
  <done>Chart helper exports a single typed contract for chart rows and available dates.</done>
</task>

<task type="auto">
  <name>Task 2: Add route handlers for chart snapshots and data status summaries</name>
  <files>src/lib/data-status.ts, src/app/api/charts/route.ts, src/app/api/data-status/route.ts</files>
  <read_first>src/app/api/health/route.ts, billboard_stats/services/data_status_service.py, src/lib/charts.ts, src/lib/db.ts, .planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md</read_first>
  <action>Create `src/lib/data-status.ts` to mirror the Python `get_data_summary()` behavior with counts for `chart_weeks`, `hot100_entries`, `b200_entries`, `songs`, `albums`, `artists`, `song_stats`, `album_stats`, and `artist_stats`, plus grouped latest chart dates. Then implement `src/app/api/charts/route.ts` and `src/app/api/data-status/route.ts`. The charts route must validate `chart` against `hot-100|billboard-200` and optionally accept `date=YYYY-MM-DD`, returning `{ chartType, selectedDate, latestDate, availableDates, entries }`. The data-status route must return `{ counts, latestDates, chart_weeks, songs, albums, artists, hot100_entries, b200_entries }`. Invalid query input should return `400` JSON, and database failures should return a concise `500` JSON response.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/app/api/charts/route.ts contains `selectedDate`
    - src/app/api/charts/route.ts contains `availableDates`
    - src/app/api/charts/route.ts contains `400`
    - src/app/api/data-status/route.ts contains `latestDates`
    - src/lib/data-status.ts contains `chart_weeks`
  </acceptance_criteria>
  <done>The UI can query chart snapshots and data status through stable internal JSON APIs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] `/api/charts` and `/api/data-status` route files exist and use shared lib helpers
</verification>

<success_criteria>

- All tasks completed
- Chart and data-status APIs are implemented with validation
- No errors or warnings introduced
- Shared data helpers exist for Phase 2 UI consumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-browse-latest-charts/02-02-SUMMARY.md`
</output>
