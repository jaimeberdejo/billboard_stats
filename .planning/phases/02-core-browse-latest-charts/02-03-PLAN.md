---
phase: 02-core-browse-latest-charts
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app/page.tsx
  - src/app/status/page.tsx
  - src/components/charts/latest-charts-view.tsx
  - src/components/charts/chart-table.tsx
  - src/components/charts/chart-controls.tsx
  - src/components/status/data-status-panel.tsx
autonomous: true
requirements:
  - CORE-04
  - BROWSE-01
  - BROWSE-02
  - BROWSE-03
  - BROWSE-04
  - BROWSE-05
must_haves:
  truths:
    - The Latest Charts page renders Hot 100 and Billboard 200 data in dense ranked tables with movement indicators.
    - Users can switch chart type and historical week without leaving the page.
    - The Data Status page shows live DB counts and the latest chart dates.
  artifacts:
    - src/app/page.tsx
    - src/app/status/page.tsx
    - src/components/charts/latest-charts-view.tsx
    - src/components/charts/chart-table.tsx
    - src/components/charts/chart-controls.tsx
    - src/components/status/data-status-panel.tsx
  key_links:
    - Latest Charts UI consumes the chart API/helper contract from Plan 02-02.
    - Page output renders inside the shared shell from Plan 02-01.
---

<objective>
Implement the actual Latest Charts and Data Status experience on top of the Phase 2 shell and APIs.

Purpose: Deliver the first user-visible browse flow with real chart data, week switching, movement badges, and DB freshness/status visibility.
Output: A server-first Latest Charts page, chart interaction components, and a live Data Status page.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-browse-latest-charts/02-RESEARCH.md
@.planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md
@.planning/phases/02-core-browse-latest-charts/02-02-PLAN.md
@src/app/page.tsx
@src/app/status/page.tsx
@BillboardStats.html
</context>

<threat_model>
<trust_boundaries>
- Server-rendered pages pass initial data into client interaction components.
- Client chart/week selection triggers internal fetches back to app-owned APIs.
</trust_boundaries>
<stride_register>
- threat_id: TM-02-05
  category: denial-of-service
  component: chart switching UI
  disposition: mitigate
  mitigation_plan: reuse the route-safe chart helper contract and avoid duplicate full-page fetches on every interaction.
- threat_id: TM-02-06
  category: information-disclosure
  component: data status page
  disposition: mitigate
  mitigation_plan: expose only aggregate counts and latest chart dates; do not surface raw database errors or credentials.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the server-first Latest Charts page and client controls</name>
  <files>src/app/page.tsx, src/components/charts/latest-charts-view.tsx, src/components/charts/chart-controls.tsx</files>
  <read_first>AGENTS.md, src/app/page.tsx, src/components/shell/site-shell.tsx, src/lib/charts.ts, src/app/api/charts/route.ts, .planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md, BillboardStats.html, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md</read_first>
  <action>Replace the current Phase 1 landing page with a Latest Charts implementation. Keep the page server-first by loading the newest Hot 100 payload on the server, then pass it into a client `LatestChartsView` that owns chart switching and week selection. Create `chart-controls.tsx` with the exact labels `HOT 100` and `B200`, a compact week selector, and the entry-count text aligned on the same control row. The view should request new snapshots through the internal charts route or shared helper contract without remounting the whole shell. Preserve the dense newsroom styling from the UI-SPEC instead of using hero cards or marketing copy.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `LatestChartsView`
    - src/components/charts/chart-controls.tsx contains `HOT 100`
    - src/components/charts/chart-controls.tsx contains `B200`
    - src/components/charts/latest-charts-view.tsx contains `availableDates`
  </acceptance_criteria>
  <done>The homepage renders the latest chart with compact controls and supports chart/week selection.</done>
</task>

<task type="auto">
  <name>Task 2: Render dense chart tables, movement badges, and live data-status views</name>
  <files>src/components/charts/chart-table.tsx, src/components/status/data-status-panel.tsx, src/app/status/page.tsx</files>
  <read_first>src/components/charts/latest-charts-view.tsx, src/lib/data-status.ts, src/app/api/data-status/route.ts, .planning/phases/02-core-browse-latest-charts/02-UI-SPEC.md, BillboardStats.html</read_first>
  <action>Create `chart-table.tsx` as a semantic table with sticky headers, compact `POS / MV / TITLE / ARTIST / LW / PK / WKS` structure, rank emphasis for `#1`, and movement indicators using green up, red down, neutral flat, plus `NEW` and `RE` badges. Create `data-status-panel.tsx` to render latest chart dates and aggregate table counts in the compact read-only style defined in the prototype. Replace the placeholder `src/app/status/page.tsx` with a live Data Status page that consumes the status helper or route output and presents both a top stats bar and a full counts table. Keep Search and Records placeholders intact; only Latest Charts and Data Status become live in this phase.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/components/charts/chart-table.tsx contains `NEW`
    - src/components/charts/chart-table.tsx contains `RE`
    - src/components/charts/chart-table.tsx contains `WKS`
    - src/components/status/data-status-panel.tsx contains `latestDates`
    - src/app/status/page.tsx does not contain `Available in Phase 4`
  </acceptance_criteria>
  <done>The browse table and data-status views match the prototype’s density and use live data contracts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] Latest Charts supports chart toggle, week selection, and movement indicators
- [ ] Data Status page displays latest chart dates and row counts
</verification>

<success_criteria>

- All tasks completed
- Latest Charts page displays Hot 100/B200 data with movement badges
- Users can change chart type and week without shell regressions
- Data Status exposes latest dates and aggregate counts accurately
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-browse-latest-charts/02-03-SUMMARY.md`
</output>
