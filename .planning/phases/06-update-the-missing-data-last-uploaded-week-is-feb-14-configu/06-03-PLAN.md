---
phase: 06-update-the-missing-data-last-uploaded-week-is-feb-14-configu
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .github/workflows/weekly-etl.yml
autonomous: false
requirements:
  - CORE-02
  - BROWSE-05

must_haves:
  truths:
    - "Weekly ETL automation exists independently of the Telegram bot runtime."
    - "The automation runs the same hardened updater used for manual backfills."
    - "Operators can trigger the automation manually and on a weekly schedule through GitHub Actions."
  artifacts:
    - path: ".github/workflows/weekly-etl.yml"
      provides: "Headless weekly ETL automation against Neon"
  key_links:
    - from: ".github/workflows/weekly-etl.yml"
      to: "scripts/run_weekly_etl.sh"
      via: "scheduled/manual workflow execution"
      pattern: "schedule|workflow_dispatch"
    - from: ".github/workflows/weekly-etl.yml"
      to: "billboard_stats/etl/updater.py"
      via: "python -m billboard_stats.etl.updater"
      pattern: "updater"
---

<objective>
Automate the weekly Neon ETL through GitHub Actions.

Purpose: The repo already has a bot-only scheduler hook, but the production data pipeline needs an infrastructure-level weekly automation path that can run without the Telegram bot process. This plan creates that standalone automation.

Output: A GitHub Actions workflow with `schedule` and `workflow_dispatch` that runs the hardened ETL updater against Neon using repository secrets.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-CONTEXT.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-01-SUMMARY.md
@scripts/run_weekly_etl.sh
@requirements.txt
@billboard_stats/etl/updater.py
</context>

<threat_model>
<trust_boundaries>
- GitHub-hosted CI runners will access Neon using repository or environment secrets.
- Scheduled automation will run without an interactive operator watching every execution.
- Workflow configuration must stay generic enough for CI while not leaking credentials or coupling to a local machine path.
</trust_boundaries>
<stride_register>
- threat_id: TM-06-05
  category: information-disclosure
  component: CI secrets
  disposition: mitigate
  mitigation_plan: load Neon connection values from GitHub Actions secrets only; never commit credentials or echo them in logs.
- threat_id: TM-06-06
  category: availability
  component: weekly automation
  disposition: mitigate
  mitigation_plan: provide both `schedule` and `workflow_dispatch`, fail loudly on updater errors, and use the same script/entrypoint as manual operations to reduce divergence.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the GitHub Actions workflow that runs the weekly ETL against Neon</name>
  <files>.github/workflows/weekly-etl.yml</files>
  <read_first>AGENTS.md, requirements.txt, scripts/run_weekly_etl.sh, billboard_stats/etl/updater.py, .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md</read_first>
  <action>Create `.github/workflows/weekly-etl.yml` with both `schedule` and `workflow_dispatch`. The workflow should check out the repo, set up Python, install `requirements.txt`, inject the ETL connection env vars from GitHub secrets, and execute the canonical ETL runner. Keep the workflow small and deterministic so local manual operations and CI automation share the same code path.</action>
  <verify>python - <<'PY'\nimport yaml, pathlib\npath = pathlib.Path('.github/workflows/weekly-etl.yml')\nprint('exists=', path.exists())\nprint('top_keys=', list(yaml.safe_load(path.read_text()).keys()))\nPY</verify>
  <acceptance_criteria>
    - .github/workflows/weekly-etl.yml contains `schedule:`
    - .github/workflows/weekly-etl.yml contains `workflow_dispatch:`
    - .github/workflows/weekly-etl.yml runs the canonical ETL entrypoint rather than duplicating business logic inline
  </acceptance_criteria>
  <done>The repo contains a production-grade weekly automation workflow for the ETL.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Configure GitHub Actions secrets for Neon ETL access</name>
  <read_first>
    - .github/workflows/weekly-etl.yml
    - billboard_stats/.env
  </read_first>
  <what-built>
    Add the required repository or environment secrets so the workflow can connect to Neon without hardcoded credentials.
  </what-built>
  <how-to-verify>
    In GitHub repo settings, add the secrets referenced by `.github/workflows/weekly-etl.yml` for:
    - `PGHOST`
    - `PGPORT`
    - `PGDATABASE`
    - `PGUSER`
    - `PGPASSWORD`
    - `PGSSLMODE`

    Expected:
    - all required secrets exist
    - values match the Neon ETL connection, with `PGSSLMODE=require`
  </how-to-verify>
  <resume-signal>Type "done" when the workflow secrets are configured, or describe what is blocked.</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Prove the workflow runs successfully through workflow_dispatch</name>
  <read_first>
    - .github/workflows/weekly-etl.yml
    - scripts/run_weekly_etl.sh
  </read_first>
  <what-built>
    Trigger a manual GitHub Actions run and confirm the job completes against Neon.
  </what-built>
  <how-to-verify>
    In GitHub Actions:

    1. Open the `weekly-etl` workflow.
    2. Trigger `Run workflow`.
    3. Watch the logs through completion.

    Expected:
    - checkout, Python setup, dependency install, and updater execution all pass
    - no missing-secret or database-connection failures occur
    - the final logs show the updater result summary
  </how-to-verify>
  <resume-signal>Type "approved" with the workflow run result, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Workflow YAML parses and exists in `.github/workflows/`
- [ ] Manual `workflow_dispatch` run succeeds
- [ ] Weekly schedule is enabled and uses the same ETL path as local manual runs
</verification>

<success_criteria>
- All tasks completed
- Weekly ETL automation exists independently of the Telegram bot
- CI and manual runs share the same updater path
- The repo is ready for unattended weekly freshness maintenance
</success_criteria>

<output>
After completion, create `.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-03-SUMMARY.md`
</output>
