---
phase: 06-update-the-missing-data-last-uploaded-week-is-feb-14-configu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - billboard_stats/etl/fetcher.py
  - billboard_stats/etl/updater.py
  - src/lib/data-status.ts
  - billboard_stats/services/data_status_service.py
autonomous: true
requirements:
  - CORE-02
  - BROWSE-05

must_haves:
  truths:
    - "The ETL no longer treats future Saturdays as valid latest chart weeks for weekly maintenance."
    - "Freshness/status queries do not report future chart dates as the latest available data."
    - "Operators have a clear, code-level way to distinguish true missing data from invalid future files."
  artifacts:
    - path: "billboard_stats/etl/updater.py"
      provides: "Bounded weekly update / repair logic based on real publishable weeks"
    - path: "billboard_stats/etl/fetcher.py"
      provides: "Date iteration that can be constrained to valid weekly ranges"
    - path: "src/lib/data-status.ts"
      provides: "Read-side freshness queries that do not trust impossible future dates"
  key_links:
    - from: "billboard_stats/etl/updater.py"
      to: "GitHub Actions weekly automation"
      via: "python -m billboard_stats.etl.updater"
      pattern: "run_update|update_charts|repair_gaps"
    - from: "src/lib/data-status.ts"
      to: "src/components/status/data-status-panel.tsx"
      via: "getDataSummary()"
      pattern: "MAX\\(chart_date\\)|latestDates"
---

<objective>
Harden the ETL and freshness logic so Phase 6 can safely backfill and automate weekly updates.

Purpose: The current archive is internally inconsistent: Hot 100 stops at 2026-02-14 while Billboard 200 contains future-dated late-2026 files. Before any backfill or automation can be trusted, the updater and status surfaces must stop treating impossible dates as real latest data.

Output: ETL date-boundary logic and data-status queries that only recognize valid chart weeks, plus a clear operational path for identifying real missing data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-CONTEXT.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md
@billboard_stats/etl/fetcher.py
@billboard_stats/etl/updater.py
@billboard_stats/etl/json_parser.py
@src/lib/data-status.ts
@billboard_stats/services/data_status_service.py
</context>

<threat_model>
<trust_boundaries>
- Billboard chart dates cross from remote third-party data into the local JSON archive and then into Neon.
- Freshness/status queries cross from the database into public app surfaces.
- Weekly automation will later rely on this updater logic without a human manually reviewing every date.
</trust_boundaries>
<stride_register>
- threat_id: TM-06-01
  category: tampering
  component: future-dated chart files or rows
  disposition: mitigate
  mitigation_plan: constrain weekly updater date ranges to publishable weeks and make freshness queries ignore future chart dates.
- threat_id: TM-06-02
  category: integrity
  component: operational freshness reporting
  disposition: mitigate
  mitigation_plan: base “latest” reporting on valid dates only so status panels and verification steps cannot be fooled by impossible rows.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bound the ETL to valid weekly date ranges instead of blindly iterating the whole current year</name>
  <files>billboard_stats/etl/fetcher.py, billboard_stats/etl/updater.py</files>
  <read_first>AGENTS.md, billboard_stats/etl/fetcher.py, billboard_stats/etl/updater.py, billboard_stats/etl/json_parser.py, .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-CONTEXT.md, .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md</read_first>
  <action>Add shared date-boundary helpers so weekly maintenance uses the latest publishable chart Saturday, not “all Saturdays in the current year”. Update the updater/fetcher flow so incremental Hot 100 and Billboard 200 maintenance cannot download or consider dates after the valid weekly boundary for the current run. Preserve historical backfill behavior where needed, but make the operational path deterministic and safe for automation.</action>
  <verify>python -m compileall billboard_stats/etl</verify>
  <acceptance_criteria>
    - billboard_stats/etl/updater.py contains a helper or logic path for a valid latest weekly boundary
    - billboard_stats/etl/updater.py no longer uses the raw current year alone as the operational upper bound for Billboard 200 maintenance
    - billboard_stats/etl/fetcher.py exposes or uses bounded date iteration compatible with weekly maintenance
  </acceptance_criteria>
  <done>The ETL has an explicit concept of “latest valid week” and will not operationally drift into future dates.</done>
</task>

<task type="auto">
  <name>Task 2: Make app and service freshness queries ignore impossible future chart dates</name>
  <files>src/lib/data-status.ts, billboard_stats/services/data_status_service.py</files>
  <read_first>AGENTS.md, src/lib/data-status.ts, billboard_stats/services/data_status_service.py, src/components/status/data-status-panel.tsx, .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md</read_first>
  <action>Update both the Next.js and legacy Python freshness helpers so “latest chart date” only considers valid non-future chart weeks. Keep the response contract stable for the UI, but ensure `/status`-style surfaces cannot report fake late-2026 freshness if invalid rows or files exist. If a small shared query comment or guard improves maintainability, add it.</action>
  <verify>npm run lint -- 'src/lib/data-status.ts' 'src/components/status/data-status-panel.tsx'</verify>
  <acceptance_criteria>
    - src/lib/data-status.ts contains a future-date guard in the latest-date query path
    - billboard_stats/services/data_status_service.py contains an equivalent future-date guard
    - the `latestDates` response shape remains unchanged for existing callers
  </acceptance_criteria>
  <done>Freshness surfaces stay truthful even if bad future data is present in the archive or database.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall billboard_stats/etl billboard_stats/services`
- [ ] `npm run lint -- 'src/lib/data-status.ts' 'src/components/status/data-status-panel.tsx'`
- [ ] The updater and status layers both use the same future-date safety assumption
</verification>

<success_criteria>
- All tasks completed
- Weekly ETL logic is bounded to valid chart weeks
- Freshness/status surfaces cannot report impossible future data
- Phase 6 operational work can proceed without being poisoned by bad chronology
</success_criteria>

<output>
After completion, create `.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-01-SUMMARY.md`
</output>
