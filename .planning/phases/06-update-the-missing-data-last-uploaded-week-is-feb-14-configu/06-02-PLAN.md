---
phase: 06-update-the-missing-data-last-uploaded-week-is-feb-14-configu
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - README.md
  - scripts/run_weekly_etl.sh
autonomous: false
requirements:
  - CORE-02
  - BROWSE-05

must_haves:
  truths:
    - "The missing production chart data after 2026-02-14 is backfilled into Neon."
    - "Operators have one committed runbook and one committed shell entrypoint for manual ETL runs."
    - "The live app freshness surfaces show the corrected latest dates after the backfill."
  artifacts:
    - path: "scripts/run_weekly_etl.sh"
      provides: "Repeatable manual ETL entrypoint for local and CI use"
    - path: "README.md"
      provides: "Backfill and weekly ETL operations instructions"
  key_links:
    - from: "scripts/run_weekly_etl.sh"
      to: "billboard_stats/etl/updater.py"
      via: "python -m billboard_stats.etl.updater"
      pattern: "updater"
    - from: "README.md"
      to: "production /api/data-status verification"
      via: "Phase 6 ETL runbook"
      pattern: "data-status|weekly ETL|backfill"
---

<objective>
Backfill the missing chart data into Neon and document the operator workflow for repeatable manual ETL execution.

Purpose: After Plan 06-01 fixes the chronology bug, the production database still needs to be brought current from the known 2026-02-14 Hot 100 stopping point. This plan also leaves behind the committed runbook and shell entrypoint that future operators and automation can reuse.

Output: Missing Neon data loaded, live freshness verified, and a committed manual ETL run path (`scripts/run_weekly_etl.sh` + docs).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-CONTEXT.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md
@.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-01-SUMMARY.md
@billboard_stats/etl/updater.py
@billboard_stats/db/connection.py
@billboard_stats/.env
@README.md
</context>

<threat_model>
<trust_boundaries>
- Manual operator commands cross from the local shell into the production Neon database.
- Freshness verification crosses from the live public app into deployment acceptance.
- Runbook instructions will later be executed by humans and CI without reinterpretation.
</trust_boundaries>
<stride_register>
- threat_id: TM-06-03
  category: integrity
  component: production backfill run
  disposition: mitigate
  mitigation_plan: use one committed shell entrypoint and one documented verification sequence instead of ad hoc commands.
- threat_id: TM-06-04
  category: information-disclosure
  component: ETL credentials
  disposition: mitigate
  mitigation_plan: source credentials from gitignored env files / CI secrets only; do not commit secrets into docs or scripts.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a committed manual ETL runner and runbook for Neon backfills and weekly maintenance</name>
  <files>scripts/run_weekly_etl.sh, README.md</files>
  <read_first>AGENTS.md, billboard_stats/etl/updater.py, billboard_stats/db/connection.py, .env.example, README.md, .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-RESEARCH.md</read_first>
  <action>Create `scripts/run_weekly_etl.sh` as the canonical shell wrapper for exporting the ETL environment and running `python -m billboard_stats.etl.updater`. Make it safe for both manual local use and later CI reuse. Then update `README.md` with a short Phase 6 operations section covering prerequisites, how to run the backfill/update, and how to verify freshness afterward through the app/API.</action>
  <verify>chmod +x scripts/run_weekly_etl.sh && bash -n scripts/run_weekly_etl.sh</verify>
  <acceptance_criteria>
    - scripts/run_weekly_etl.sh contains `python -m billboard_stats.etl.updater`
    - scripts/run_weekly_etl.sh does not hardcode secrets
    - README.md contains a discoverable weekly ETL / backfill section
  </acceptance_criteria>
  <done>The repo contains a repeatable operator path for manual ETL runs and verification.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Run the hardened ETL against Neon to backfill the missing production data</name>
  <read_first>
    - scripts/run_weekly_etl.sh
    - billboard_stats/.env
    - .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-01-SUMMARY.md
  </read_first>
  <what-built>
    Execute the canonical ETL updater against the Neon database so the gap after 2026-02-14 is repaired using the hardened chronology logic from Plan 06-01.
  </what-built>
  <how-to-verify>
    From the project root:

    ```
    chmod +x scripts/run_weekly_etl.sh
    ./scripts/run_weekly_etl.sh
    ```

    Expected:
    - the script loads the ETL env safely
    - `python -m billboard_stats.etl.updater` completes without crashing
    - the result summary shows nonzero repaired and/or updated weeks if the production DB was stale

    Capture the printed result summary.
  </how-to-verify>
  <resume-signal>Type "done" with the updater result summary, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify the live app now reports corrected freshness and serves current chart data</name>
  <read_first>
    - README.md (new Phase 6 ETL section)
    - .planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-01-SUMMARY.md
  </read_first>
  <what-built>
    Verify that the production app reports truthful, up-to-date chart freshness after the backfill.
  </what-built>
  <how-to-verify>
    Run:

    ```
    curl -s https://billboard-stats.vercel.app/api/data-status
    curl -s "https://billboard-stats.vercel.app/api/charts?chart=hot-100" | head -c 200
    curl -s "https://billboard-stats.vercel.app/api/charts?chart=billboard-200" | head -c 200
    ```

    Expected:
    - `/api/data-status` reports non-future latest dates
    - Hot 100 latest date is later than `2026-02-14`
    - both chart APIs return real payloads, not empty results or configuration errors
  </how-to-verify>
  <resume-signal>Type "approved" with the latest dates and verification outcome, or describe discrepancies.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/run_weekly_etl.sh`
- [ ] README documents both execution and verification
- [ ] Human checkpoints confirm the live data is fresh and non-future
</verification>

<success_criteria>
- All tasks completed
- Production Neon data is backfilled past the 2026-02-14 gap
- Manual ETL operations are documented and repeatable
- Live freshness surfaces report corrected dates
</success_criteria>

<output>
After completion, create `.planning/phases/06-update-the-missing-data-last-uploaded-week-is-feb-14-configu/06-02-SUMMARY.md`
</output>
