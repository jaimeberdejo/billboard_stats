---
phase: 04-search-records
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/records/records-view.tsx
  - src/components/records/leaderboard-list.tsx
  - src/components/records/artist-drilldown.tsx
  - src/app/records/page.tsx
autonomous: true
requirements:
  - RECORDS-01
must_haves:
  truths:
    - Records preset leaderboards expose the complete backend-supported record list, not only a demo subset.
    - Chart-specific unsupported record types fail with informative inline messaging.
    - Artist-scoped leaderboards can expand inline drilldowns while song/album rows still navigate directly.
  artifacts:
    - src/components/records/records-view.tsx
    - src/components/records/leaderboard-list.tsx
    - src/components/records/artist-drilldown.tsx
    - src/app/records/page.tsx
  key_links:
    - src/components/records/records-view.tsx consumes the records API preset and drilldown modes from Plan 04-01.
    - src/app/records/page.tsx swaps the placeholder route for the Phase 4 records interface shell.
---

<objective>
Build the preset records and drilldown experience.

Purpose: Replace the Records placeholder with the full leaderboard surface before layering on the custom query builder.
Output: Records page client island, leaderboard list renderer, inline artist drilldowns, and route integration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-records/04-CONTEXT.md
@.planning/phases/04-search-records/04-RESEARCH.md
@.planning/phases/04-search-records/04-UI-SPEC.md
@.planning/phases/04-search-records/04-01-PLAN.md
@src/app/records/page.tsx
@BillboardStats.html
@billboard_stats/app.py
</context>

<threat_model>
<trust_boundaries>
- Records control selections cross from the browser into the records API route.
- Records drilldown expansions expose derived item subsets tied to an artist and optional week.
</trust_boundaries>
<stride_register>
- threat_id: TM-04-05
  category: tampering
  component: preset/drilldown selections
  disposition: mitigate
  mitigation_plan: keep record type, chart, and drilldown identifiers constrained to typed route-safe values from the API contract.
- threat_id: TM-04-06
  category: denial-of-service
  component: leaderboard interaction
  disposition: mitigate
  mitigation_plan: keep the records client island local-state driven and reuse already-loaded row metadata when toggling drilldowns.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the Records client view, leaderboard list, and artist drilldown components</name>
  <files>src/components/records/records-view.tsx, src/components/records/leaderboard-list.tsx, src/components/records/artist-drilldown.tsx</files>
  <read_first>AGENTS.md, .planning/phases/04-search-records/04-UI-SPEC.md, .planning/phases/04-search-records/04-CONTEXT.md, BillboardStats.html, billboard_stats/app.py, src/components/charts/chart-table.tsx</read_first>
  <action>Create `src/components/records/records-view.tsx` as a `"use client"` component managing the selected record type, selected chart, expanded artist row, pending state, and fetches for preset leaderboards plus drilldowns. Create `src/components/records/leaderboard-list.tsx` to render the dense leaderboard rows with rank, title, optional artist/date meta, value label, and hover-only emphasis. Create `src/components/records/artist-drilldown.tsx` for inline expansion rows that surface artist-specific songs or albums for the drilldown-enabled presets. The preset selector must include `Custom Query`, `Most Weeks at #1`, `Longest Chart Runs`, `Most #1 Songs (by Artist)`, `Most #1 Albums (by Artist)`, `Most Entries by Artist`, `Most Simultaneous Entries`, `Biggest Debuts`, and `Fastest to #1`.</action>
  <verify>npm run lint -- 'src/components/records/records-view.tsx' 'src/components/records/leaderboard-list.tsx' 'src/components/records/artist-drilldown.tsx'</verify>
  <acceptance_criteria>
    - src/components/records/records-view.tsx contains `"use client"`
    - src/components/records/records-view.tsx contains `Most Simultaneous Entries`
    - src/components/records/records-view.tsx contains `Fastest to #1`
    - src/components/records/leaderboard-list.tsx contains `title`
    - src/components/records/artist-drilldown.tsx contains `/song/`
  </acceptance_criteria>
  <done>Records has reusable interactive components for presets and drilldowns.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Records placeholder route with the preset leaderboard experience</name>
  <files>src/app/records/page.tsx</files>
  <read_first>AGENTS.md, src/app/records/page.tsx, src/components/records/records-view.tsx, .planning/phases/04-search-records/04-UI-SPEC.md, BillboardStats.html, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md</read_first>
  <action>Update `src/app/records/page.tsx` to remove the Phase 2 placeholder and instead render the Records heading plus the `RecordsView` client island. Preserve the route title `Records — Billboard Stats`, keep the page shell server-rendered except for the client island, and ensure the page shows informative inline states for unsupported combinations such as `Most Simultaneous Entries` on `billboard-200`.</action>
  <verify>npm run lint -- 'src/app/records/page.tsx'</verify>
  <acceptance_criteria>
    - src/app/records/page.tsx contains `RecordsView`
    - src/app/records/page.tsx does not contain `Available in Phase 4`
    - src/app/records/page.tsx contains `Records — Billboard Stats`
  </acceptance_criteria>
  <done>The Records route renders the real preset leaderboard interface instead of a placeholder.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- 'src/components/records/records-view.tsx' 'src/components/records/leaderboard-list.tsx' 'src/components/records/artist-drilldown.tsx' 'src/app/records/page.tsx'`
- [ ] `npm run build`
- [ ] Records presets render the full supported list with drilldowns and unsupported-state messaging
</verification>

<success_criteria>
- All tasks completed
- Records preset leaderboards are accurate and interactive
- Artist-scoped drilldowns stay inline while direct entity rows still navigate
- The route no longer uses a placeholder panel
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-records/04-03-SUMMARY.md`
</output>
