---
phase: 04-search-records
plan: 04
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-03"
files_modified:
  - src/components/records/custom-query-builder.tsx
  - src/components/records/records-view.tsx
autonomous: true
requirements:
  - RECORDS-02
must_haves:
  truths:
    - The custom query builder uses the prototype’s sentence-style interaction model instead of a generic form stack.
    - Filters for artist, peak range, debut range, and minimum weeks are supported through the typed records API.
    - Custom query results return top-50 leaderboard rows without breaking the preset records flow.
  artifacts:
    - src/components/records/custom-query-builder.tsx
    - src/components/records/records-view.tsx
  key_links:
    - src/components/records/custom-query-builder.tsx consumes the records API custom mode from Plan 04-01.
    - src/components/records/records-view.tsx hosts both preset records and custom-query flows in one consistent shell.
---

<objective>
Add the natural-language custom query builder to the Records experience.

Purpose: Complete the last interactive requirement for the milestone without regressing the preset leaderboards already shipped in Plan 04-03.
Output: Custom query builder component and full integration into the Records client view.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-records/04-CONTEXT.md
@.planning/phases/04-search-records/04-RESEARCH.md
@.planning/phases/04-search-records/04-UI-SPEC.md
@.planning/phases/04-search-records/04-03-PLAN.md
@src/components/records/records-view.tsx
@BillboardStats.html
@billboard_stats/app.py
</context>

<threat_model>
<trust_boundaries>
- Custom query control values cross from the browser into the records API route.
- Numeric sentence-builder controls and optional filters determine leaderboard results returned to the user.
</trust_boundaries>
<stride_register>
- threat_id: TM-04-07
  category: tampering
  component: custom query numeric and filter inputs
  disposition: mitigate
  mitigation_plan: constrain all sentence-builder numeric values and filter bounds to chart-safe ranges before sending them to the API.
- threat_id: TM-04-08
  category: denial-of-service
  component: custom query refetch cycle
  disposition: mitigate
  mitigation_plan: keep the builder in one local-state client island, fetch only when the chosen controls change, and cap the rendered results to the top 50 rows.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the natural-language custom query builder component</name>
  <files>src/components/records/custom-query-builder.tsx</files>
  <read_first>AGENTS.md, .planning/phases/04-search-records/04-UI-SPEC.md, .planning/phases/04-search-records/04-CONTEXT.md, BillboardStats.html, billboard_stats/app.py</read_first>
  <action>Create `src/components/records/custom-query-builder.tsx` as a `"use client"` component rendering the sentence-style query builder from the prototype. It must let the user choose the sort direction, rank mode, optional rank parameter, artist filter, peak range, debut range, and minimum weeks. The chart-aware rank parameter must stay within `1..100` for `hot-100` and `1..200` for `billboard-200`. The UI must preserve the dense inline tone of the prototype rather than using stacked form sections.</action>
  <verify>npm run lint -- 'src/components/records/custom-query-builder.tsx'</verify>
  <acceptance_criteria>
    - src/components/records/custom-query-builder.tsx contains `"use client"`
    - src/components/records/custom-query-builder.tsx contains `Show me`
    - src/components/records/custom-query-builder.tsx contains `most weeks`
    - src/components/records/custom-query-builder.tsx contains `top`
  </acceptance_criteria>
  <done>The Records experience has a reusable sentence-style custom query builder component.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate the custom query builder into the Records client view and preserve top-50 results</name>
  <files>src/components/records/records-view.tsx</files>
  <read_first>AGENTS.md, src/components/records/records-view.tsx, src/components/records/custom-query-builder.tsx, .planning/phases/04-search-records/04-UI-SPEC.md, BillboardStats.html, billboard_stats/app.py</read_first>
  <action>Update `src/components/records/records-view.tsx` so choosing `Custom Query` swaps in the new `CustomQueryBuilder`, sends the chosen controls to the records API `custom` mode, and renders the top 50 returned rows with the existing leaderboard list. Preserve the preset records selector and chart toggle flow from Plan 04-03, keep the result count visible, and ensure switching away from `Custom Query` returns the page to the preset leaderboard behavior without stale drilldown state.</action>
  <verify>npm run lint -- 'src/components/records/records-view.tsx'</verify>
  <acceptance_criteria>
    - src/components/records/records-view.tsx contains `Custom Query`
    - src/components/records/records-view.tsx contains `custom`
    - src/components/records/records-view.tsx contains `50`
    - src/components/records/records-view.tsx contains `result`
  </acceptance_criteria>
  <done>The Records page supports the prototype-style custom query workflow on top of the preset leaderboard shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- 'src/components/records/custom-query-builder.tsx' 'src/components/records/records-view.tsx'`
- [ ] `npm run build`
- [ ] Custom query builder returns top-50 filtered results without breaking preset records
</verification>

<success_criteria>
- All tasks completed
- Custom query builder matches the prototype’s interaction style
- Records filters and rank modes map cleanly to the typed API contract
- The final milestone requirement for records queries is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-records/04-04-SUMMARY.md`
</output>
