---
phase: 04-search-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/search.ts
  - src/lib/records.ts
  - src/app/api/search/route.ts
  - src/app/api/records/route.ts
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - RECORDS-01
  - RECORDS-02
must_haves:
  truths:
    - Search and records both expose typed server-safe contracts before any client UI depends on them.
    - Search returns grouped song, album, and artist results from one validated request.
    - Records exposes preset, custom, and drilldown modes through one validated API contract.
  artifacts:
    - src/lib/search.ts
    - src/lib/records.ts
    - src/app/api/search/route.ts
    - src/app/api/records/route.ts
  key_links:
    - src/app/search/page.tsx will consume the grouped search contract through a client fetch path.
    - src/app/records/page.tsx will consume the records API for presets, custom queries, and drilldowns.
---

<objective>
Create the backend contracts and API routes for Phase 4 Search and Records.

Purpose: Stabilize the TypeScript translation of the existing Python search and records services before the interactive UI is built on top.
Output: Typed lib helpers and validated API routes for grouped search, preset records, custom record queries, and artist drilldown data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-records/04-CONTEXT.md
@.planning/phases/04-search-records/04-RESEARCH.md
@src/lib/db.ts
@src/app/api/charts/route.ts
@billboard_stats/services/song_service.py
@billboard_stats/services/album_service.py
@billboard_stats/services/artist_service.py
@billboard_stats/services/records_service.py
@billboard_stats/models/schemas.py
</context>

<threat_model>
<trust_boundaries>
- Search and records query params cross from the browser into internal API routes.
- API routes cross into PostgreSQL through the Neon-backed server helpers.
- Search and records responses expose public analytical data back to the browser.
</trust_boundaries>
<stride_register>
- threat_id: TM-04-01
  category: tampering
  component: search query and records mode params
  disposition: mitigate
  mitigation_plan: validate search length, chart values, record modes, numeric filters, and drilldown IDs against explicit allowlists and numeric bounds before any helper call.
- threat_id: TM-04-02
  category: information-disclosure
  component: API error responses
  disposition: mitigate
  mitigation_plan: return concise user-safe JSON errors without leaking SQL text, connection details, or stack traces.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Port grouped fuzzy search into typed TypeScript helpers and a validated search API route</name>
  <files>src/lib/search.ts, src/app/api/search/route.ts</files>
  <read_first>AGENTS.md, src/lib/db.ts, src/app/api/charts/route.ts, billboard_stats/services/song_service.py, billboard_stats/services/album_service.py, billboard_stats/services/artist_service.py, .planning/phases/04-search-records/04-CONTEXT.md, .planning/phases/04-search-records/04-RESEARCH.md</read_first>
  <action>Create `src/lib/search.ts` exporting concrete grouped search contracts for songs, albums, and artists, plus a `searchAll(query: string)` helper that returns one payload containing `songs`, `albums`, and `artists`. Reuse the existing PostgreSQL trigram search semantics from the Python services, enforce the minimum-length guard at the route layer, limit each entity group to 50 results, and include the exact fields required by the planned dense tables: songs/albums need `id`, `title`, `artist_credit`, `peak_position`, `total_weeks`, and `weeks_at_peak`; artists need `id`, `name`, `total_hot100_songs`, `total_b200_albums`, `hot100_number_ones`, and `b200_number_ones`. Then add `src/app/api/search/route.ts` validating `q`, returning `400` for queries shorter than 2 chars, and surfacing a grouped JSON payload with concise safe error messages.</action>
  <verify>npm run lint -- 'src/lib/search.ts' 'src/app/api/search/route.ts'</verify>
  <acceptance_criteria>
    - src/lib/search.ts contains `export async function searchAll(`
    - src/lib/search.ts contains `songs:`
    - src/lib/search.ts contains `albums:`
    - src/lib/search.ts contains `artists:`
    - src/app/api/search/route.ts contains `q`
    - src/app/api/search/route.ts contains `shorter than 2 characters`
  </acceptance_criteria>
  <done>Search has one stable server-side contract and one validated API route ready for the client UI.</done>
</task>

<task type="auto">
  <name>Task 2: Port records, custom query, and drilldown behavior into typed helpers and a validated records API route</name>
  <files>src/lib/records.ts, src/app/api/records/route.ts</files>
  <read_first>AGENTS.md, src/lib/db.ts, src/app/api/charts/route.ts, billboard_stats/services/records_service.py, billboard_stats/models/schemas.py, .planning/phases/04-search-records/04-CONTEXT.md, .planning/phases/04-search-records/04-RESEARCH.md</read_first>
  <action>Create `src/lib/records.ts` as the TypeScript translation of `records_service.py`. Export typed contracts for preset rows, artist drilldown rows, and custom query parameters/results. Support preset modes for `most-weeks-at-number-one`, `longest-chart-runs`, `most-number-one-songs-by-artist`, `most-number-one-albums-by-artist`, `most-entries-by-artist`, `most-simultaneous-entries`, `biggest-debuts`, and `fastest-to-number-one`. Support custom-query rank modes for weeks at `#1`, total weeks, weeks at a specific position, and weeks in the top N, with filters for artist names, peak range, debut range, and minimum total weeks. Then create `src/app/api/records/route.ts` validating `mode`, `chart`, numeric bounds, and drilldown IDs. The route must support three explicit modes: `preset`, `custom`, and `drilldown`, and return informative safe messages for unsupported chart/record combinations.</action>
  <verify>npm run lint -- 'src/lib/records.ts' 'src/app/api/records/route.ts'</verify>
  <acceptance_criteria>
    - src/lib/records.ts contains `preset`
    - src/lib/records.ts contains `custom`
    - src/lib/records.ts contains `drilldown`
    - src/lib/records.ts contains `most-simultaneous-entries`
    - src/app/api/records/route.ts contains `mode`
    - src/app/api/records/route.ts contains `unsupported`
  </acceptance_criteria>
  <done>Records has typed backend contracts for presets, custom queries, and drilldowns, exposed through one validated route.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- 'src/lib/search.ts' 'src/lib/records.ts' 'src/app/api/search/route.ts' 'src/app/api/records/route.ts'`
- [ ] `npm run build`
- [ ] Search and records contracts are explicitly named and route-safe
</verification>

<success_criteria>
- All tasks completed
- Search and records data access is typed, validated, and server-safe
- Phase 4 UI can consume one stable contract per interaction mode
- Unsupported records combinations fail gracefully instead of silently
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-records/04-01-SUMMARY.md`
</output>
