---
phase: 04-search-records
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/search/search-view.tsx
  - src/components/search/search-results-table.tsx
  - src/app/search/page.tsx
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
must_haves:
  truths:
    - Search begins only after 2 characters and shows grouped result counts in tabs.
    - Search results preserve the dense table language already established elsewhere in the app.
    - Search rows navigate directly into Phase 3 detail pages.
  artifacts:
    - src/components/search/search-view.tsx
    - src/components/search/search-results-table.tsx
    - src/app/search/page.tsx
  key_links:
    - src/components/search/search-view.tsx consumes the grouped `/api/search` payload from Plan 04-01.
    - src/app/search/page.tsx swaps the Phase 2 placeholder for the real Phase 4 client island.
---

<objective>
Build the real Search page experience.

Purpose: Replace the placeholder Search route with a prototype-faithful, tabbed fuzzy-search experience.
Output: Search page client island, shared dense results table, and route integration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-records/04-CONTEXT.md
@.planning/phases/04-search-records/04-RESEARCH.md
@.planning/phases/04-search-records/04-UI-SPEC.md
@.planning/phases/04-search-records/04-01-PLAN.md
@src/app/search/page.tsx
@src/components/charts/chart-table.tsx
@src/components/detail/stats-bar.tsx
@BillboardStats.html
@billboard_stats/app.py
</context>

<threat_model>
<trust_boundaries>
- Search input crosses from the browser into the internal search API route.
- Search results surface public entity metadata and stats back to the browser.
</trust_boundaries>
<stride_register>
- threat_id: TM-04-03
  category: denial-of-service
  component: live fuzzy search
  disposition: mitigate
  mitigation_plan: keep the client island lightweight, avoid requests for queries under 2 characters, and preserve the 50-result cap.
- threat_id: TM-04-04
  category: spoofing
  component: search result navigation
  disposition: mitigate
  mitigation_plan: derive `/song/{id}`, `/album/{id}`, and `/artist/{id}` navigation targets from typed API payload IDs only.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the reusable Search client view and dense result table renderer</name>
  <files>src/components/search/search-view.tsx, src/components/search/search-results-table.tsx</files>
  <read_first>AGENTS.md, .planning/phases/04-search-records/04-UI-SPEC.md, .planning/phases/04-search-records/04-CONTEXT.md, src/components/charts/chart-table.tsx, BillboardStats.html, billboard_stats/app.py</read_first>
  <action>Create `src/components/search/search-view.tsx` as a `"use client"` component that manages the query string, active tab, pending state, and fetches from `/api/search`. The component must not fire until the query reaches 2 characters, must show `Type at least 2 characters to search.` before then, and must render tab labels in the order `Songs`, `Albums`, `Artists` with inline counts after the query becomes valid. Create `src/components/search/search-results-table.tsx` as the dense reusable renderer for the three result types. Song and album tables must render columns `TITLE`, `ARTIST`, `PK`, `WKS`, and `WKS@PK`; artist tables must render `NAME`, `SONGS`, `ALBUMS`, `#1 SNG`, and `#1 ALB`. Use hover-only emphasis and route to the typed detail URLs on row click.</action>
  <verify>npm run lint -- 'src/components/search/search-view.tsx' 'src/components/search/search-results-table.tsx'</verify>
  <acceptance_criteria>
    - src/components/search/search-view.tsx contains `"use client"`
    - src/components/search/search-view.tsx contains `Type at least 2 characters to search.`
    - src/components/search/search-view.tsx contains `Songs`
    - src/components/search/search-view.tsx contains `Albums`
    - src/components/search/search-view.tsx contains `Artists`
    - src/components/search/search-results-table.tsx contains `WKS@PK`
  </acceptance_criteria>
  <done>Search has a reusable interactive client view and dense table renderer.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Search placeholder route with the real Phase 4 search experience</name>
  <files>src/app/search/page.tsx</files>
  <read_first>AGENTS.md, src/app/search/page.tsx, src/components/search/search-view.tsx, .planning/phases/04-search-records/04-UI-SPEC.md, BillboardStats.html, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md</read_first>
  <action>Update `src/app/search/page.tsx` to remove the placeholder “Available in Phase 4” panel and instead render the Search heading plus the new `SearchView` client island. Preserve the current page-shell spacing and heading language, keep the page server-rendered except for the interactive search component, and ensure the final route title remains `Search — Billboard Stats`.</action>
  <verify>npm run lint -- 'src/app/search/page.tsx'</verify>
  <acceptance_criteria>
    - src/app/search/page.tsx contains `SearchView`
    - src/app/search/page.tsx does not contain `Available in Phase 4`
    - src/app/search/page.tsx contains `Search — Billboard Stats`
  </acceptance_criteria>
  <done>The Search route renders the real Phase 4 search interface instead of a placeholder.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- 'src/components/search/search-view.tsx' 'src/components/search/search-results-table.tsx' 'src/app/search/page.tsx'`
- [ ] `npm run build`
- [ ] Search only requests data after 2 characters and shows tabbed counts
</verification>

<success_criteria>
- All tasks completed
- Search page feels prototype-faithful and navigates cleanly into detail pages
- Search uses the grouped search contract rather than ad hoc per-tab fetches
- Dense table language stays consistent with the rest of the app
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-records/04-02-SUMMARY.md`
</output>
