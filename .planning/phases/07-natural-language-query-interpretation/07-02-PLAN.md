---
phase: 07-natural-language-query-interpretation
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/lib/nlq/interpret.ts
  - src/lib/nlq/explain.ts
  - src/app/api/query/route.ts
autonomous: true
requirements:
  - SEARCH-01
  - RECORDS-02
must_haves:
  truths:
    - "A plain-English question returns a structured interpretation with explicit status and intent instead of executing search or records queries."
    - "Search-style questions preserve the existing fuzzy-search minimum length and grouped entity semantics."
    - "Records-style questions resolve only to preset or custom-record shapes the existing backend already supports, or they return clarification/unsupported output."
  artifacts:
    - path: "src/lib/nlq/interpret.ts"
      provides: "Deterministic intent classification and parameter extraction."
    - path: "src/lib/nlq/explain.ts"
      provides: "User-visible explanation and warning text derived from interpreted objects."
    - path: "src/app/api/query/route.ts"
      provides: "Interpretation-only route surface for GET/POST requests."
      exports:
        - "GET"
        - "POST"
  key_links:
    - from: "src/app/api/query/route.ts"
      to: "src/lib/nlq/interpret.ts"
      via: "route handler passes request text into the deterministic interpreter"
      pattern: "interpretQuery"
    - from: "src/lib/nlq/interpret.ts"
      to: "src/lib/nlq/schema.ts"
      via: "final output is validated before leaving the interpreter"
      pattern: "interpretedQuerySchema"
    - from: "src/lib/nlq/interpret.ts"
      to: "src/lib/nlq/explain.ts"
      via: "interpretQuery must build explanation, warnings, and ambiguity text through the shared explanation helper before returning the API-safe object"
      pattern: "buildInterpretationExplanation"
---

<objective>
Implement the deterministic natural-language interpreter, explanation builder, and API boundary for Phase 7.

Purpose: Convert raw user questions into explicit `ok`, `needs_clarification`, or `unsupported` interpretations without touching the database or execution helpers.
Output: `interpret.ts`, `explain.ts`, and `src/app/api/query/route.ts`.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md
@.planning/phases/07-natural-language-query-interpretation/07-RESEARCH.md
@.planning/phases/07-natural-language-query-interpretation/07-PATTERNS.md
@src/lib/search.ts
@src/lib/records.ts
@src/app/api/search/route.ts
@src/app/api/records/route.ts
@src/lib/nlq/schema.ts
@src/lib/nlq/catalog.ts
@src/lib/nlq/normalize.ts

<interfaces>
Interpreter output contract created in Plan `07-01`:
```typescript
type InterpretationStatus = "ok" | "needs_clarification" | "unsupported";
type InterpretationIntent =
  | "search"
  | "records_preset"
  | "records_custom"
  | "clarify"
  | "unsupported";
```

Route pattern from `src/app/api/records/route.ts`:
```typescript
export async function GET(request: NextRequest): Promise<Response> {
  // validate request
  // return Response.json(payload)
  // catch and return generic 500
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the deterministic intent classifier and extraction pipeline</name>
  <read_first>
    AGENTS.md
    src/lib/nlq/schema.ts
    src/lib/nlq/catalog.ts
    src/lib/nlq/normalize.ts
    src/lib/search.ts
    src/lib/records.ts
    src/app/api/records/route.ts
    src/components/records/custom-query-builder.tsx
    .planning/phases/07-natural-language-query-interpretation/07-RESEARCH.md
    .planning/phases/07-natural-language-query-interpretation/07-PATTERNS.md
  </read_first>
  <files>src/lib/nlq/interpret.ts, src/lib/nlq/explain.ts</files>
  <action>Create `src/lib/nlq/interpret.ts` exporting `interpretQuery(question: string): InterpretedQuery` and any small private helpers needed to: normalize the question, classify `search` vs `records_preset` vs `records_custom` vs `clarify` vs `unsupported`, extract `chart`, `entity`, `rankBy`, `rankByParam`, artist filters, peak/debut/weeks filters, and reject out-of-scope asks such as date filters or unsupported domains. Use the exact Phase 4 preset slugs and custom-record fields. Make the interpreter explicitly enforce the same semantic compatibility rules already implied by `src/components/records/custom-query-builder.tsx` and `src/lib/records.ts`: songs map only to `hot-100`, albums map only to `billboard-200`, artist queries are the only ones allowed to switch chart context directly, song/album queries may use only `weeks-at-number-one`, `weeks-at-position`, `weeks-in-top-n`, and `total-weeks`, artist queries may use only `total-weeks`, `most-entries`, and chart-specific `number-one-entries`, and preset/chart combinations already marked unsupported downstream must return `status: "unsupported"` or `status: "needs_clarification"` instead of emitting an invalid structured query object. Search interpretations must only become `status: "ok"` when the extracted query text is at least 2 characters; otherwise emit `status: "needs_clarification"` with an ambiguity reason about the 2-character minimum. Create `src/lib/nlq/explain.ts` exporting helpers that synthesize deterministic `explanation`, `warnings`, and `ambiguityReasons` strings from the structured interpretation object, and require `interpretQuery` to populate those returned fields by calling the shared `buildInterpretationExplanation` path rather than composing explanation text separately.</action>
  <acceptance_criteria>
    `src/lib/nlq/interpret.ts` contains `export function interpretQuery`
    `src/lib/nlq/interpret.ts` contains `status: "needs_clarification"`
    `src/lib/nlq/interpret.ts` contains `status: "unsupported"`
    `src/lib/nlq/interpret.ts` contains `interpretedQuerySchema`
    `src/lib/nlq/interpret.ts` contains `buildInterpretationExplanation`
    `src/lib/nlq/interpret.ts` contains `entity === "songs"`
    `src/lib/nlq/interpret.ts` contains `entity === "albums"`
    `src/lib/nlq/explain.ts` contains `export function buildInterpretationExplanation`
    `src/lib/nlq/interpret.ts` does not contain `searchAll(`
    `src/lib/nlq/interpret.ts` does not contain `getCustomRecords(`
    `src/lib/nlq/interpret.ts` does not contain `getPresetRecords(`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'export function interpretQuery|status: \"needs_clarification\"|status: \"unsupported\"|interpretedQuerySchema|buildInterpretationExplanation' src/lib/nlq/interpret.ts src/lib/nlq/explain.ts && ! rg -n 'searchAll\\(|getCustomRecords\\(|getPresetRecords\\(' src/lib/nlq/interpret.ts src/lib/nlq/explain.ts && npm run lint -- src/lib/nlq/interpret.ts src/lib/nlq/explain.ts</automated>
  </verify>
  <done>The interpreter can classify and explain supported search/records questions while returning bounded clarification or unsupported outputs for everything else, and it never emits semantically invalid entity/chart/rank combinations for Phase 8 to execute.</done>
</task>

<task type="auto">
  <name>Task 2: Add the interpretation-only API route</name>
  <read_first>
    AGENTS.md
    node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md
    src/app/api/search/route.ts
    src/app/api/records/route.ts
    src/lib/nlq/interpret.ts
    src/lib/nlq/explain.ts
    src/lib/nlq/schema.ts
  </read_first>
  <files>src/app/api/query/route.ts</files>
  <action>Create `src/app/api/query/route.ts` with `GET` and `POST` handlers that accept a raw question from `q` or `{ question }`, trim it, require non-empty input, call `interpretQuery`, and return the structured interpretation object as JSON. Follow the existing route pattern for concise `400` errors and generic `500` errors. Ensure the route never imports `searchAll`, `getPresetRecords`, `getCustomRecords`, or `getSql`, because Phase 7 must stop before execution.</action>
  <acceptance_criteria>
    `src/app/api/query/route.ts` contains `export async function GET`
    `src/app/api/query/route.ts` contains `export async function POST`
    `src/app/api/query/route.ts` contains `interpretQuery`
    `src/app/api/query/route.ts` contains `Invalid or missing`
    `src/app/api/query/route.ts` does not contain `searchAll`
    `src/app/api/query/route.ts` does not contain `getPresetRecords`
    `src/app/api/query/route.ts` does not contain `getCustomRecords`
    `src/app/api/query/route.ts` does not contain `getSql`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'export async function GET|export async function POST|interpretQuery|Invalid or missing' src/app/api/query/route.ts && ! rg -n 'searchAll|getPresetRecords|getCustomRecords|getSql' src/app/api/query/route.ts && npm run lint -- src/app/api/query/route.ts && npm run build</automated>
  </verify>
  <done>The app exposes one backend-owned interpretation endpoint that returns only structured NLQ output and preserves the Phase 7 execution boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/api/query` | Untrusted freeform text reaches the interpreter and route validation layer. |
| interpreter -> future executor | The returned object becomes the only allowed handoff into Phase 8 execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | E | `src/lib/nlq/interpret.ts` | mitigate | Keep interpretation deterministic and schema-validated so freeform text cannot escalate into arbitrary operations or off-contract fields. |
| T-07-04 | D | `src/app/api/query/route.ts` | mitigate | Reject empty input early and keep the route CPU-only with no database calls, so malformed requests fail fast with bounded work. |
</threat_model>

<verification>
Run the task-level lint/build commands and confirm the route and interpreter contain no search/records execution imports.
</verification>

<success_criteria>
Submitting a supported chart question to `/api/query` returns a structured interpretation, while ambiguous or unsupported questions return explicit non-executing statuses and explanations.
</success_criteria>

<output>
After completion, create `.planning/phases/07-natural-language-query-interpretation/07-natural-language-query-interpretation-02-SUMMARY.md`
</output>
