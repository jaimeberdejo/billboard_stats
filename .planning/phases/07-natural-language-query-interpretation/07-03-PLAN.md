---
phase: 07-natural-language-query-interpretation
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/lib/nlq/fixtures.ts
  - scripts/verify-nlq-fixtures.ts
autonomous: true
requirements:
  - SEARCH-01
  - RECORDS-02
must_haves:
  truths:
    - "The supported interpretation grammar is locked by executable golden cases for search, records preset, records custom, clarification, and unsupported flows."
    - "Explanation and warning text stay aligned with the structured interpretation object instead of drifting independently."
    - "Phase 7 verification proves interpretation behavior without executing database-backed search or records queries."
  artifacts:
    - path: "src/lib/nlq/fixtures.ts"
      provides: "Golden query fixtures with expected structured interpretation outputs."
    - path: "scripts/verify-nlq-fixtures.ts"
      provides: "Executable regression script for Phase 7 interpretation behavior."
  key_links:
    - from: "scripts/verify-nlq-fixtures.ts"
      to: "src/lib/nlq/interpret.ts"
      via: "fixture runner asserts exact status, intent, and extracted field values"
      pattern: "interpretQuery"
    - from: "scripts/verify-nlq-fixtures.ts"
      to: "src/lib/nlq/explain.ts"
      via: "fixture runner checks explanation and warnings against expected snippets"
      pattern: "buildInterpretationExplanation"
---

<objective>
Lock the Phase 7 interpretation behavior with executable golden fixtures.

Purpose: Make the bounded vocabulary, clarification rules, and explanation strings regression-testable before Phase 8 execution and Phase 9 UI build on top of them.
Output: `fixtures.ts` and an executable verification script under `scripts/`.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/07-natural-language-query-interpretation/07-RESEARCH.md
@.planning/phases/07-natural-language-query-interpretation/07-PATTERNS.md
@src/lib/nlq/schema.ts
@src/lib/nlq/catalog.ts
@src/lib/nlq/normalize.ts
@src/lib/nlq/interpret.ts
@src/lib/nlq/explain.ts
@src/app/api/query/route.ts

<interfaces>
Expected interpreter surface from Plan `07-02`:
```typescript
export function interpretQuery(question: string): InterpretedQuery;
export function buildInterpretationExplanation(query: InterpretedQuery): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the golden interpretation fixture corpus</name>
  <read_first>
    AGENTS.md
    src/lib/nlq/schema.ts
    src/lib/nlq/catalog.ts
    src/lib/nlq/interpret.ts
    src/lib/nlq/explain.ts
    .planning/phases/07-natural-language-query-interpretation/07-RESEARCH.md
    .planning/phases/07-natural-language-query-interpretation/07-PATTERNS.md
  </read_first>
  <files>src/lib/nlq/fixtures.ts</files>
  <action>Create `src/lib/nlq/fixtures.ts` exporting a typed fixture array such as `GOLDEN_QUERY_FIXTURES` with exact expected outputs for at least: one short search that becomes `needs_clarification`, one valid mixed-entity search, one preset-record question, one custom-record question with extracted `chart` and `rankByParam`, one unsupported out-of-scope question, one ambiguous question that needs clarification, and explicit invalid-combination cases covering entity/chart mismatch, entity/rankBy mismatch, and preset/chart combinations already treated as unsupported downstream. Each fixture must store the raw question, expected `status`, expected `intent`, expected structured branch fields, and expected explanation or warning snippets so the verification script can assert deterministic behavior and confirm the interpreter never emits semantically invalid records contracts.</action>
  <acceptance_criteria>
    `src/lib/nlq/fixtures.ts` contains `export const GOLDEN_QUERY_FIXTURES`
    `src/lib/nlq/fixtures.ts` contains `needs_clarification`
    `src/lib/nlq/fixtures.ts` contains `records_preset`
    `src/lib/nlq/fixtures.ts` contains `records_custom`
    `src/lib/nlq/fixtures.ts` contains `unsupported`
    `src/lib/nlq/fixtures.ts` contains `entity/chart`
    `src/lib/nlq/fixtures.ts` contains `preset/chart`
    `src/lib/nlq/fixtures.ts` contains `expectedExplanationIncludes`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'GOLDEN_QUERY_FIXTURES|needs_clarification|records_preset|records_custom|unsupported|expectedExplanationIncludes' src/lib/nlq/fixtures.ts && npm run lint -- src/lib/nlq/fixtures.ts</automated>
  </verify>
  <done>The phase has an explicit golden corpus that covers every interpretation status, both requirement families, and the invalid records combinations that Phase 8 must never be asked to execute.</done>
</task>

<task type="auto">
  <name>Task 2: Add an executable regression script for fixture verification</name>
  <read_first>
    AGENTS.md
    src/lib/nlq/fixtures.ts
    src/lib/nlq/interpret.ts
    src/lib/nlq/explain.ts
    src/app/api/query/route.ts
  </read_first>
  <files>scripts/verify-nlq-fixtures.ts</files>
  <action>Create `scripts/verify-nlq-fixtures.ts` as a zero-network executable script that imports the fixture corpus and interpreter, runs every question through `interpretQuery`, asserts exact `status` and `intent`, compares expected extracted fields branch-by-branch, and checks that generated explanation/warning text contains the required snippets. Make the script set a non-zero exit code when any fixture fails and print the failing fixture name and mismatched field. Keep it interpretation-only: do not fetch `/api/query`, do not import DB helpers, and do not call any execution route.</action>
  <acceptance_criteria>
    `scripts/verify-nlq-fixtures.ts` contains `interpretQuery`
    `scripts/verify-nlq-fixtures.ts` contains `process.exitCode = 1`
    `scripts/verify-nlq-fixtures.ts` contains `GOLDEN_QUERY_FIXTURES`
    `scripts/verify-nlq-fixtures.ts` does not contain `getSql`
    `scripts/verify-nlq-fixtures.ts` does not contain `fetch(`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'interpretQuery|process.exitCode = 1|GOLDEN_QUERY_FIXTURES' scripts/verify-nlq-fixtures.ts && ! rg -n 'getSql|fetch\\(' scripts/verify-nlq-fixtures.ts && npm run lint -- scripts/verify-nlq-fixtures.ts && node --experimental-strip-types scripts/verify-nlq-fixtures.ts</automated>
  </verify>
  <done>Phase 7 has an executable regression command that proves interpretation and explanation behavior without crossing into search or records execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture corpus -> interpreter | Test data must exercise only supported interpretation paths and unsupported/clarify fallbacks. |
| verification script -> CI/local execution | The verification runner must remain hermetic and avoid accidental network or DB access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | R | `src/lib/nlq/fixtures.ts` | mitigate | Store exact expected status, intent, branch fields, and explanation snippets so regressions are attributable to named fixtures. |
| T-07-06 | D | `scripts/verify-nlq-fixtures.ts` | mitigate | Keep the verifier local-only with direct interpreter imports and explicit rejection of DB/network helpers, ensuring the check stays fast and deterministic. |
</threat_model>

<verification>
Run the fixture script directly and confirm it passes without fetching `/api/query` or importing any database-backed modules.
</verification>

<success_criteria>
Any change that alters supported intent detection, extraction, or explanation behavior causes the Phase 7 fixture runner to fail with a precise mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/07-natural-language-query-interpretation/07-natural-language-query-interpretation-03-SUMMARY.md`
</output>
