---
phase: 05-i-want-to-configure-neon-and-deploy-the-app-in-vercel
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - billboard_stats/.env
  - .env.example
autonomous: false
requirements:
  - CORE-02
  - CORE-03

must_haves:
  truths:
    - "All live routes return real data (not empty responses or DATABASE_URL errors)"
    - "Python ETL can write to Neon when PGHOST/PGDATABASE/PGUSER/PGPASSWORD/PGSSLMODE are exported"
    - ".env.example documents the DATABASE_URL format used by this deployment"
    - "The app URL is confirmed and the milestone is shippable"
  artifacts:
    - path: "billboard_stats/.env"
      provides: "ETL Neon connection env file (gitignored via .env* pattern)"
      contains: "PGHOST=ep-"
    - path: ".env.example"
      provides: "Updated template documenting pooled Neon DATABASE_URL format with sslmode=require"
      contains: "sslmode=require"
  key_links:
    - from: "billboard_stats/.env"
      to: "billboard_stats/db/connection.py _get_setting()"
      via: "PGHOST / PGDATABASE / PGUSER / PGPASSWORD / PGSSLMODE env vars"
      pattern: "PGHOST=ep-.*neon\\.tech"
    - from: "production URL /api/charts"
      to: "Neon database"
      via: "getSql() + DATABASE_URL env var"
      pattern: "HTTP 200.*\\[\\{"
---

<objective>
Run smoke tests against the live production URL to confirm all key routes return real data, update the Python ETL to point at Neon, and update .env.example to document the correct DATABASE_URL format.

Purpose: This is the acceptance gate for the deployment. Data-empty pages or DATABASE_URL errors mean the deployment is not complete. The Python ETL cutover ensures the ETL pipeline writes to the same Neon database the app reads from.

Output: A verified live deployment, ETL pointing at Neon, and updated documentation in .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-CONTEXT.md
@.planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-RESEARCH.md
@.planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-01-SUMMARY.md
@.planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-02-SUMMARY.md
@billboard_stats/db/connection.py
@.env.example

<interfaces>
<!-- Python ETL connection.py reads these env vars (from billboard_stats/db/connection.py): -->
```python
host=_get_setting("PGHOST", "localhost"),
port=int(_get_setting("PGPORT", "5432")),
dbname=_get_setting("PGDATABASE", "billboard"),
user=_get_setting("PGUSER", "postgres"),
password=_get_setting("PGPASSWORD", ""),
# sslmode only set if PGSSLMODE is non-empty:
sslmode=_get_setting("PGSSLMODE")  # must be "require" for Neon
```
<!-- No code changes needed — only env vars. -->

<!-- Parse these values from the UNPOOLED Neon connection string: -->
<!-- postgresql://USER:PASS@ep-<id>.us-east-1.aws.neon.tech/neondb?sslmode=require -->
<!--   PGHOST=ep-<id>.us-east-1.aws.neon.tech  (no -pooler suffix) -->
<!--   PGPORT=5432 -->
<!--   PGDATABASE=neondb -->
<!--   PGUSER=USER -->
<!--   PGPASSWORD=PASS -->
<!--   PGSSLMODE=require -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Smoke-test all key routes on the live production URL</name>
  <read_first>
    - .planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-02-SUMMARY.md (to get the production URL)
    - .planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-RESEARCH.md (section: Post-Deploy Verification)
  </read_first>
  <what-built>
    Run the smoke-test suite against the live production URL to confirm all routes return real data from Neon.
  </what-built>
  <how-to-verify>
    Replace `<PROD_URL>` with your actual production URL (from 05-02-SUMMARY.md).

    **Step 1 — HTTP status check:**
    ```
    curl -I https://<PROD_URL>/
    curl -I https://<PROD_URL>/charts
    curl -I https://<PROD_URL>/search
    curl -I https://<PROD_URL>/records
    ```
    Expected: All return `HTTP/2 200` (or HTTP/1.1 200).

    **Step 2 — Data API check:**
    ```
    curl -s "https://<PROD_URL>/api/charts" | head -c 200
    ```
    Expected: JSON array starting with `[{"` containing chart data. NOT an empty array `[]` and NOT `{"error":"DATABASE_URL is not configured."}`.

    **Step 3 — Search API check:**
    ```
    curl -s "https://<PROD_URL>/api/search?q=taylor" | head -c 200
    ```
    Expected: JSON with songs/albums/artists results for "taylor".

    **Step 4 — Browser check (open these URLs):**
    - `https://<PROD_URL>/charts` — Hot 100 table with real song data and movement indicators
    - `https://<PROD_URL>/search` — Search interface loads
    - `https://<PROD_URL>/records` — Leaderboards render with data
    - `https://<PROD_URL>/artist/1` — Artist detail page renders with career stats

    **Step 5 — Check Vercel runtime logs for errors:**
    ```
    vercel logs --environment production --level error --since 5m
    ```
    Expected: No error logs. If errors appear, check the message — "DATABASE_URL is not configured" means the env var wasn't picked up at build time. Re-run `vercel env add DATABASE_URL production` and redeploy.
  </how-to-verify>
  <resume-signal>Type "approved" with the production URL confirmed and all routes verified, or describe any failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create ETL .env file and update .env.example</name>
  <read_first>
    - .planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-01-SUMMARY.md (to get the UNPOOLED Neon connection string and parsed credentials)
    - billboard_stats/db/connection.py (to confirm env var names are correct before writing .env)
    - .env.example (to see current content before updating)
    - .gitignore (to confirm .env* is excluded — already verified: line `.env*`)
  </read_first>
  <action>
    **1. Create `billboard_stats/.env` for Python ETL use.**

    Read the UNPOOLED Neon connection string from 05-01-SUMMARY.md:
    `postgresql://USER:PASS@ep-<id>.us-east-1.aws.neon.tech/neondb`

    Parse the components:
    - PGHOST = `ep-<id>.us-east-1.aws.neon.tech` (the host part, no -pooler suffix)
    - PGPORT = `5432`
    - PGDATABASE = `neondb`
    - PGUSER = the user from the connection string
    - PGPASSWORD = the password from the connection string
    - PGSSLMODE = `require` (CRITICAL — Neon requires SSL; without this the ETL will fail to connect)

    Write `billboard_stats/.env` with these exact variable names (connection.py reads them via `_get_setting()`):
    ```
    PGHOST=ep-<id>.us-east-1.aws.neon.tech
    PGPORT=5432
    PGDATABASE=neondb
    PGUSER=<user-from-connection-string>
    PGPASSWORD=<pass-from-connection-string>
    PGSSLMODE=require
    ```

    Use the real values from 05-01-SUMMARY.md — NOT placeholder text.

    **2. Update `.env.example` in the project root.**

    Current content: `DATABASE_URL=postgresql://user:password@host.neon.tech/neondb`

    Replace the entire file content with the following (do NOT add real credentials — this is committed to git):
    ```
    # Next.js app (Neon pooled connection — used by @neondatabase/serverless)
    DATABASE_URL=postgresql://user:password@ep-pooler-name-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require

    # Python ETL pipeline (Neon direct/unpooled connection — used by billboard_stats/db/connection.py)
    # Set these before running the ETL. Values come from the unpooled Neon connection string.
    PGHOST=ep-<id>.us-east-1.aws.neon.tech
    PGPORT=5432
    PGDATABASE=neondb
    PGUSER=<neon-user>
    PGPASSWORD=<neon-password>
    PGSSLMODE=require
    ```

    Do NOT add real credentials to .env.example — it is committed to git.

    **3. Verify .gitignore covers billboard_stats/.env:**
    The `.env*` pattern in the root `.gitignore` covers root-level `.env` files.
    Check whether `billboard_stats/.env` is also excluded:
    ```bash
    git check-ignore -v billboard_stats/.env
    ```
    If the output shows the file is NOT ignored, add a `.gitignore` entry for `billboard_stats/.env` by appending to the root `.gitignore`:
    ```
    billboard_stats/.env
    ```
    Or verify if `billboard_stats/` has its own `.gitignore`.

    The ETL `.env` file MUST be gitignored — it contains the Neon database password.
  </action>
  <verify>
    <automated>
      # 1. ETL .env file exists with required variables:
      grep -E "^PGHOST=ep-" /Users/jaimeberdejosanchez/projects/billboard_stats/billboard_stats/.env

      # 2. PGSSLMODE is set to require:
      grep -E "^PGSSLMODE=require$" /Users/jaimeberdejosanchez/projects/billboard_stats/billboard_stats/.env

      # 3. .env.example has sslmode=require in the DATABASE_URL:
      grep "sslmode=require" /Users/jaimeberdejosanchez/projects/billboard_stats/.env.example

      # 4. billboard_stats/.env is gitignored:
      cd /Users/jaimeberdejosanchez/projects/billboard_stats && git check-ignore -v billboard_stats/.env
    </automated>
  </verify>
  <done>
    - `billboard_stats/.env` exists with all six ETL env vars (PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD, PGSSLMODE=require), using the UNPOOLED Neon hostname (no `-pooler` suffix)
    - `git check-ignore -v billboard_stats/.env` shows the file is gitignored
    - `.env.example` contains `sslmode=require` in the DATABASE_URL template and uses the pooled URL format
    - `.env.example` also contains the ETL variable block: PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD, PGSSLMODE lines with placeholder values
    - No real credentials appear in `.env.example`
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify ETL can connect to Neon and test end-to-end ETL connection</name>
  <read_first>
    - billboard_stats/db/connection.py (to understand how env vars are loaded)
    - .planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-RESEARCH.md (section: Python ETL Cutover)
    - billboard_stats/.env (just created in Task 2 — check it looks correct before running)
  </read_first>
  <what-built>
    Verify the Python ETL connects to Neon using the env vars written in Task 2. No code changes are needed — only env var export.
  </what-built>
  <how-to-verify>
    From the project root:

    **Step 1 — Export env vars from the new .env file:**
    ```
    set -a && source billboard_stats/.env && set +a
    ```
    Verify they loaded:
    ```
    echo $PGHOST
    ```
    Expected: `ep-<id>.us-east-1.aws.neon.tech` (NOT "localhost").

    **Step 2 — Test connection with psql using the same env vars:**
    ```
    export PATH="/opt/homebrew/opt/postgresql@16/bin:$PATH"
    psql -h $PGHOST -p $PGPORT -d $PGDATABASE -U $PGUSER -c "SELECT COUNT(*) FROM chart_weeks;" sslmode=$PGSSLMODE
    ```
    Or equivalently using the PGPASSWORD env var (psql reads it automatically):
    ```
    PGPASSWORD=$PGPASSWORD psql -h $PGHOST -p $PGPORT -d $PGDATABASE -U $PGUSER \
      -c "SELECT COUNT(*) FROM chart_weeks;"
    ```
    Expected: returns `7073` (or close to it — same as the row count from Plan 01).

    **Step 3 — Test Python ETL connection:**
    If the project has a Python ETL entry point (e.g., `billboard_stats/etl/run.py` or similar), run a dry-run or connection test. Otherwise, run a minimal Python snippet:
    ```
    python3 -c "
    import os, sys
    sys.path.insert(0, '.')
    # Load env vars (already exported above)
    from billboard_stats.db.connection import execute_query
    result = execute_query('SELECT COUNT(*) as cnt FROM chart_weeks')
    print('Connected to Neon. chart_weeks count:', result[0]['cnt'])
    "
    ```
    Expected: `Connected to Neon. chart_weeks count: 7073`

    If the import path fails, try from within the billboard_stats directory:
    ```
    cd billboard_stats
    python3 -c "from db.connection import execute_query; r = execute_query('SELECT 1 as ok'); print(r)"
    cd ..
    ```
    Expected: `[{'ok': 1}]`

    **Step 4 — Confirm no localhost connection remains as default:**
    ```
    echo $PGHOST
    ```
    Must print the Neon hostname, NOT "localhost".
  </how-to-verify>
  <resume-signal>Type "approved" when the Python ETL connects to Neon and returns the correct chart_weeks count, or describe any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| billboard_stats/.env → Python ETL | Credentials file on local filesystem; must be gitignored |
| .env.example → git | Template file committed to git; must NOT contain real credentials |
| Public production URL | All routes are read-only public — no authenticated data surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | billboard_stats/.env credentials committed to git | mitigate | Task 2 explicitly checks `git check-ignore -v billboard_stats/.env` and adds gitignore entry if missing. Acceptance criteria require gitignore confirmation before task is done. |
| T-05-11 | Information Disclosure | Real credentials accidentally written to .env.example | mitigate | Task 2 acceptance criteria require `grep` of .env.example confirms NO real credentials (only placeholder text). `.env.example` is committed to git. |
| T-05-12 | Repudiation | ETL writes to Neon without audit trail | accept | ETL is a local tool run manually by the developer. No multi-user access. For v1.0, no audit logging is needed. |
| T-05-13 | Denial of Service | ETL psycopg2 pool (maxconn=10) plus Neon free tier connection limits | accept | Neon free tier supports up to 20 direct connections. ETL pool maxconn=10 is within this limit. No action needed for v1.0. |
</threat_model>

<verification>
After all three tasks complete:

1. `grep -E "^PGHOST=ep-" billboard_stats/.env` returns the Neon hostname (not localhost).
2. `grep -E "^PGSSLMODE=require$" billboard_stats/.env` returns the line.
3. `git check-ignore -v billboard_stats/.env` shows the file is gitignored.
4. `grep "sslmode=require" .env.example` returns the updated template line.
5. Python ETL connection test returns chart_weeks count matching Plan 01 row count.
6. All four production routes (/charts, /search, /records, /artist/1) return HTTP 200 with data.
7. `vercel logs --environment production --level error --since 5m` returns no errors.
</verification>

<success_criteria>
- Live production URL serves all key routes with real Neon data (per D-03: DATABASE_URL env var activates existing getSql() — confirmed working)
- Python ETL connects to Neon via env vars in `billboard_stats/.env` with PGSSLMODE=require (per D-04, D-05: connection.py already env-var driven)
- `billboard_stats/.env` is gitignored (security requirement)
- `.env.example` documents pooled connection string with `sslmode=require` (updated from placeholder)
- Milestone v1.0 is shippable: app is live, data is in Neon, ETL points at Neon
</success_criteria>

<output>
After completion, create `.planning/phases/05-i-want-to-configure-neon-and-deploy-the-app-in-vercel/05-03-SUMMARY.md`

Include in the summary:
- Final production URL
- Confirmation of all smoke-test results (routes and HTTP status codes)
- Confirmation that billboard_stats/.env is gitignored
- ETL connection test result (chart_weeks count)
- Any open issues or follow-up items (e.g., if ETL import path needed adjustment)
</output>
