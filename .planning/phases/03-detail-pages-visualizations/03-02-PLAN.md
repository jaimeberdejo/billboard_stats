---
phase: 03-detail-pages-visualizations
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/detail/detail-header.tsx
  - src/components/detail/stats-bar.tsx
  - src/components/detail/artist-pills.tsx
  - src/components/detail/chart-history-table.tsx
  - src/app/song/[id]/page.tsx
  - src/app/album/[id]/page.tsx
autonomous: true
requirements:
  - DETAILS-01
  - DETAILS-02
  - DETAILS-04
must_haves:
  truths:
    - Song and album pages render a compact header, dense stats bar, and artist pills using the approved Phase 3 newsroom styling.
    - Song and album pages render a week-by-week chart history table with newest weeks first.
    - Chart-history movement states match the Phase 2 browse semantics instead of inventing a new encoding.
  artifacts:
    - src/components/detail/detail-header.tsx
    - src/components/detail/stats-bar.tsx
    - src/components/detail/artist-pills.tsx
    - src/components/detail/chart-history-table.tsx
    - src/app/song/[id]/page.tsx
    - src/app/album/[id]/page.tsx
  key_links:
    - src/app/song/[id]/page.tsx consumes SongDetailPayload from src/lib/songs.ts.
    - src/app/album/[id]/page.tsx consumes AlbumDetailPayload from src/lib/albums.ts.
    - src/components/detail/chart-history-table.tsx reuses the movement semantics already established in src/components/charts/chart-table.tsx.
---

<objective>
Build the shared song/album detail page structure and chart-history tables.

Purpose: Deliver the primary detail-page experience before layering on the optional chart-run visualization.
Output: Shared detail UI primitives plus server-rendered song and album routes with newest-first history tables and artist pills.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages-visualizations/03-RESEARCH.md
@.planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md
@.planning/phases/03-detail-pages-visualizations/03-01-PLAN.md
@src/app/page.tsx
@src/components/charts/chart-table.tsx
@BillboardStats.html
@billboard_stats/app.py

<interfaces>
From src/components/charts/chart-table.tsx:
```ts
function getMovement(entry: ChartEntry): { label: string; tone: string }
```

Expected from Plan 03-01:
```ts
interface DetailChartRunPoint {
  chart_date: string;
  rank: number;
  last_pos: number | null;
  is_new: boolean;
  peak_pos: number | null;
  weeks_on_chart: number | null;
}
```
</interfaces>
</context>

<threat_model>
<trust_boundaries>
- Server-rendered detail pages read database-backed payloads and render them into HTML.
- Detail pages emit links to related artist routes that users can follow directly.
</trust_boundaries>
<stride_register>
- threat_id: TM-03-03
  category: spoofing
  component: related-artist navigation
  disposition: mitigate
  mitigation_plan: build artist pill hrefs from typed numeric artist IDs returned by the server helpers, not from user-provided strings.
- threat_id: TM-03-04
  category: denial-of-service
  component: detail page rendering
  disposition: mitigate
  mitigation_plan: keep song and album pages server-first and avoid client bootstrap fetches for the primary stats and history table.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared detail-page presentation primitives</name>
  <files>src/components/detail/detail-header.tsx, src/components/detail/stats-bar.tsx, src/components/detail/artist-pills.tsx</files>
  <read_first>AGENTS.md, .planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md, .planning/phases/03-detail-pages-visualizations/03-RESEARCH.md, src/components/status/data-status-panel.tsx, src/components/charts/chart-table.tsx, BillboardStats.html</read_first>
  <action>Create `src/components/detail/detail-header.tsx`, `src/components/detail/stats-bar.tsx`, and `src/components/detail/artist-pills.tsx`. `DetailHeader` must render a top `← Back` link, a main title, and a muted subtitle. It must support quoted song/album titles and plain artist titles without forcing client state. `StatsBar` must accept an array of `{ label, value, accent?: boolean }` items and render a tight bordered grid that collapses to two columns on mobile and expands across the container on larger screens. `ArtistPills` must accept an array of `{ id, name }` links, render outlined wrap-friendly pills using `next/link`, and route to `/artist/{id}` with hover-only accent treatment per the UI-SPEC. Do not use cards, icon libraries, or solid red chips.</action>
  <verify>npm run lint -- src/components/detail/detail-header.tsx src/components/detail/stats-bar.tsx src/components/detail/artist-pills.tsx</verify>
  <acceptance_criteria>
    - src/components/detail/detail-header.tsx contains `← Back`
    - src/components/detail/stats-bar.tsx contains `accent?: boolean`
    - src/components/detail/stats-bar.tsx contains `grid-cols-2`
    - src/components/detail/artist-pills.tsx contains `href={\`/artist/`
  </acceptance_criteria>
  <done>Shared detail primitives exist and match the Phase 3 compact newsroom visual contract.</done>
</task>

<task type="auto">
  <name>Task 2: Build the song and album routes with newest-first chart-history tables</name>
  <files>src/components/detail/chart-history-table.tsx, src/app/song/[id]/page.tsx, src/app/album/[id]/page.tsx</files>
  <read_first>AGENTS.md, src/app/page.tsx, src/app/status/page.tsx, src/components/charts/chart-table.tsx, src/lib/songs.ts, src/lib/albums.ts, .planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md, BillboardStats.html, billboard_stats/app.py, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md</read_first>
  <action>Create `src/components/detail/chart-history-table.tsx` as the shared detail-table renderer for songs and albums. It must accept chronological `chartRun` data and render rows newest week first, with exact columns `Week`, `Pos`, `Mv`, `Lw`, `Pk`, and `Wks`. Reuse Phase 2 movement semantics: `NEW` for debut rows, `RE` when `last_pos` is null or non-positive on a non-new row, green `▲n` for upward moves, red `▼n` for drops, and muted neutral for flat movement. Then create `src/app/song/[id]/page.tsx` and `src/app/album/[id]/page.tsx` as `force-dynamic` server routes. Each page must call its helper directly (`getSongDetail` or `getAlbumDetail`), render a graceful inline error panel on thrown DB errors, render a neutral `Song not found` or `Album not found` state when the helper returns `null`, and otherwise output the section order locked in the research and UI-SPEC: back link, title block, stats bar, `Chart History`, and `Artists`. Use the exact stats labels `Peak`, `Weeks on Chart`, `Weeks at #1`, `Weeks at Peak`, `Debut Position`, and `Debut Date`.</action>
  <verify>npm run lint -- src/components/detail/chart-history-table.tsx src/app/song/[id]/page.tsx src/app/album/[id]/page.tsx</verify>
  <acceptance_criteria>
    - src/components/detail/chart-history-table.tsx contains `Week`
    - src/components/detail/chart-history-table.tsx contains `NEW`
    - src/components/detail/chart-history-table.tsx contains `RE`
    - src/app/song/[id]/page.tsx contains `Song not found`
    - src/app/song/[id]/page.tsx contains `Chart History`
    - src/app/album/[id]/page.tsx contains `Album not found`
    - src/app/album/[id]/page.tsx contains `Artists`
  </acceptance_criteria>
  <done>Song and album routes render server-first detail pages with dense stats, movement-aware history rows, and artist links.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- src/components/detail/detail-header.tsx src/components/detail/stats-bar.tsx src/components/detail/artist-pills.tsx src/components/detail/chart-history-table.tsx src/app/song/[id]/page.tsx src/app/album/[id]/page.tsx`
- [ ] `npm run build`
- [ ] Song and album pages render newest-first history tables with the Phase 2 movement vocabulary
</verification>

<success_criteria>
- All tasks completed
- Song and album pages show compact headers, stats bars, chart history, and artist pills
- Movement treatment is consistent with Phase 2 browse views
- No client bootstrap fetch is required for the initial song or album page load
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages-visualizations/03-02-SUMMARY.md`
</output>
