---
phase: 03-detail-pages-visualizations
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/components/artist/artist-catalog-table.tsx
  - src/app/artist/[id]/page.tsx
autonomous: true
requirements:
  - DETAILS-03
must_haves:
  truths:
    - Artist detail pages render career aggregates in a dense stats bar.
    - Artist detail pages render separate drill-down tables for Hot 100 songs and Billboard 200 albums.
    - Artist drill-down rows route directly to the matching song and album detail pages.
  artifacts:
    - src/components/artist/artist-catalog-table.tsx
    - src/app/artist/[id]/page.tsx
  key_links:
    - src/app/artist/[id]/page.tsx consumes ArtistDetailPayload from src/lib/artists.ts.
    - src/components/artist/artist-catalog-table.tsx links rows to /song/[id] and /album/[id] detail routes.
---

<objective>
Build the artist detail experience with aggregate stats and drill-down catalog tables.

Purpose: Complete the entity-detail slice by adding the artist route that connects back out to song and album details.
Output: Server-rendered artist page plus a reusable dense artist catalog table component.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages-visualizations/03-RESEARCH.md
@.planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md
@.planning/phases/03-detail-pages-visualizations/03-02-PLAN.md
@src/lib/artists.ts
@src/components/detail/detail-header.tsx
@src/components/detail/stats-bar.tsx
@src/components/charts/chart-table.tsx
@BillboardStats.html
@billboard_stats/app.py
</context>

<threat_model>
<trust_boundaries>
- Artist detail pages expose aggregated stats and link out to other entity routes.
- Catalog tables convert database-backed IDs into navigable hyperlinks.
</trust_boundaries>
<stride_register>
- threat_id: TM-03-05
  category: information-disclosure
  component: artist aggregates
  disposition: mitigate
  mitigation_plan: render only the aggregate stats returned by the existing artist service contract and avoid exposing any raw query/debug data.
- threat_id: TM-03-06
  category: spoofing
  component: drill-down table links
  disposition: mitigate
  mitigation_plan: derive `/song/{id}` and `/album/{id}` hrefs from typed helper output rather than concatenating unchecked user input.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the dense artist catalog table component</name>
  <files>src/components/artist/artist-catalog-table.tsx</files>
  <read_first>AGENTS.md, src/components/charts/chart-table.tsx, src/components/detail/stats-bar.tsx, src/lib/artists.ts, .planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md, BillboardStats.html</read_first>
  <action>Create `src/components/artist/artist-catalog-table.tsx` as the shared dense table for both artist sub-sections. It must accept a section title plus an array of rows carrying `id`, `title`, `peak_position`, `total_weeks`, `weeks_at_peak`, `debut_date`, and `href`. Render exact table headers `TITLE`, `PK`, `WKS`, `WKS@PK`, and `DEBUT`. The first column must present a clickable drill-down surface that routes to the row `href`, and peak `#1` values must use the same red emphasis pattern as the Phase 2 chart table. Keep the table semantic and horizontally resilient; do not add a separate action column or card wrappers.</action>
  <verify>npm run lint -- src/components/artist/artist-catalog-table.tsx</verify>
  <acceptance_criteria>
    - src/components/artist/artist-catalog-table.tsx contains `TITLE`
    - src/components/artist/artist-catalog-table.tsx contains `WKS@PK`
    - src/components/artist/artist-catalog-table.tsx contains `href`
  </acceptance_criteria>
  <done>The artist page has a reusable dense table component for both song and album catalog sections.</done>
</task>

<task type="auto">
  <name>Task 2: Build the server-rendered artist detail route</name>
  <files>src/app/artist/[id]/page.tsx</files>
  <read_first>AGENTS.md, src/app/song/[id]/page.tsx, src/lib/artists.ts, src/components/detail/detail-header.tsx, src/components/detail/stats-bar.tsx, src/components/artist/artist-catalog-table.tsx, .planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md, BillboardStats.html, billboard_stats/app.py, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md</read_first>
  <action>Create `src/app/artist/[id]/page.tsx` as a `force-dynamic` server route that calls `getArtistDetail` directly. When the helper throws, render the approved inline error message style; when it returns `null`, render a neutral `Artist not found` state. Otherwise render the section order from the prototype: top `← Back` link, artist name header, date-range subtitle from `first_chart_date` and `latest_chart_date`, dense stats bar, `Hot 100 Songs` table, and `Billboard 200 Albums` table. Use the exact stat labels `Hot 100 Songs`, `B200 Albums`, `#1 Songs`, `#1 Albums`, `Hot 100 Weeks`, `B200 Weeks`, `Best Hot 100`, and `Max Simultaneous`. Populate drill-down rows with `/song/{id}` and `/album/{id}` hrefs. If both songs and albums are empty, render the Phase 3 neutral empty-state treatment instead of blank space.</action>
  <verify>npm run lint -- src/app/artist/[id]/page.tsx</verify>
  <acceptance_criteria>
    - src/app/artist/[id]/page.tsx contains `Artist not found`
    - src/app/artist/[id]/page.tsx contains `Hot 100 Songs`
    - src/app/artist/[id]/page.tsx contains `Billboard 200 Albums`
    - src/app/artist/[id]/page.tsx contains `Max Simultaneous`
  </acceptance_criteria>
  <done>The artist detail page renders aggregate stats plus drill-down tables linking to song and album routes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- src/components/artist/artist-catalog-table.tsx src/app/artist/[id]/page.tsx`
- [ ] `npm run build`
- [ ] Artist page shows both catalog sections when data exists and links rows to detail routes
</verification>

<success_criteria>
- All tasks completed
- Artist page exposes career aggregates and two dense drill-down tables
- Song and album row links navigate to the corresponding detail routes
- No shell or layout regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages-visualizations/03-03-SUMMARY.md`
</output>
