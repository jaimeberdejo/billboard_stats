---
phase: 03-detail-pages-visualizations
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/components/detail/chart-run-visualization.tsx
  - src/app/song/[id]/page.tsx
  - src/app/album/[id]/page.tsx
autonomous: true
requirements:
  - DETAILS-05
must_haves:
  truths:
    - Song and album pages expose a collapsible chart-run visualization above the history table.
    - The visualization renders an inline SVG with an inverted Y-axis and peak annotation.
    - The visualization is omitted when fewer than two chart points exist.
  artifacts:
    - src/components/detail/chart-run-visualization.tsx
    - src/app/song/[id]/page.tsx
    - src/app/album/[id]/page.tsx
  key_links:
    - src/components/detail/chart-run-visualization.tsx consumes the chronological chart-run data from Plan 03-01.
    - src/app/song/[id]/page.tsx and src/app/album/[id]/page.tsx render the visualization between the stats bar and chart-history table.
---

<objective>
Add the optional chart-run SVG visualization to song and album detail pages.

Purpose: Complete the Phase 3 visualization requirement without regressing the table-first information hierarchy.
Output: A small client visualization island and song/album page integration points.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages-visualizations/03-RESEARCH.md
@.planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md
@.planning/phases/03-detail-pages-visualizations/03-02-PLAN.md
@src/app/song/[id]/page.tsx
@src/app/album/[id]/page.tsx
@src/components/detail/chart-history-table.tsx
@BillboardStats.html
@billboard_stats/app.py
</context>

<threat_model>
<trust_boundaries>
- Server-rendered song and album pages pass chart-run data into a client visualization island.
- The client visualization expands and collapses without triggering extra database reads.
</trust_boundaries>
<stride_register>
- threat_id: TM-03-07
  category: denial-of-service
  component: visualization toggle
  disposition: mitigate
  mitigation_plan: keep the chart-run visualization as a lightweight local-state client island and reuse the already-loaded chart-run payload rather than fetching on toggle.
- threat_id: TM-03-08
  category: integrity
  component: chart-run SVG rendering
  disposition: mitigate
  mitigation_plan: derive SVG coordinates directly from the typed chronological chart-run points so the inverted rank scale and peak annotation match the underlying data.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the chart-run visualization client island</name>
  <files>src/components/detail/chart-run-visualization.tsx</files>
  <read_first>AGENTS.md, .planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md, .planning/phases/03-detail-pages-visualizations/03-RESEARCH.md, src/components/detail/chart-history-table.tsx, BillboardStats.html</read_first>
  <action>Create `src/components/detail/chart-run-visualization.tsx` as a `"use client"` component that owns the collapsed/expanded state and renders the inline SVG when expanded. Export `ChartRunVisualization` with props `chartType`, `points`, and `title`. The module must render nothing when `points.length < 2`. When it does render, the toggle label must include `Chart Run Visualization`, the SVG must use an inverted Y-axis with rank `1` at the top, and the Y-axis ticks must be `[1, 25, 50, 75, 100]` for `hot-100` and `[1, 50, 100, 150, 200]` for `billboard-200`. Plot the full chronological series, show only the first and last X-axis date labels, draw the line and peak dot in `#C8102E`, and annotate the best rank point with `#${rank}`. Do not add any charting library or canvas dependency.</action>
  <verify>npm run lint -- src/components/detail/chart-run-visualization.tsx</verify>
  <acceptance_criteria>
    - src/components/detail/chart-run-visualization.tsx contains `"use client"`
    - src/components/detail/chart-run-visualization.tsx contains `Chart Run Visualization`
    - src/components/detail/chart-run-visualization.tsx contains `[1, 25, 50, 75, 100]`
    - src/components/detail/chart-run-visualization.tsx contains `[1, 50, 100, 150, 200]`
    - src/components/detail/chart-run-visualization.tsx contains `#C8102E`
  </acceptance_criteria>
  <done>The chart-run component encapsulates toggle state and renders the required SVG semantics from the existing chart-run payload.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate the visualization into song and album pages without changing section order</name>
  <files>src/app/song/[id]/page.tsx, src/app/album/[id]/page.tsx</files>
  <read_first>AGENTS.md, src/app/song/[id]/page.tsx, src/app/album/[id]/page.tsx, src/components/detail/chart-run-visualization.tsx, src/components/detail/chart-history-table.tsx, .planning/phases/03-detail-pages-visualizations/03-UI-SPEC.md, BillboardStats.html</read_first>
  <action>Update `src/app/song/[id]/page.tsx` and `src/app/album/[id]/page.tsx` to render `ChartRunVisualization` immediately after the stats bar and before the `Chart History` section. Pass the chronological `chartRun` array from the detail helpers and the matching `chartType`. Keep the history table as the primary data surface: do not move it below the artists section, and do not replace the table with the graph. Only render the visualization module when at least two points exist. Preserve the existing song/album not-found and error states from Plan 03-02.</action>
  <verify>npm run lint -- src/app/song/[id]/page.tsx src/app/album/[id]/page.tsx</verify>
  <acceptance_criteria>
    - src/app/song/[id]/page.tsx contains `ChartRunVisualization`
    - src/app/album/[id]/page.tsx contains `ChartRunVisualization`
    - src/app/song/[id]/page.tsx contains `Chart History`
    - src/app/album/[id]/page.tsx contains `Chart History`
  </acceptance_criteria>
  <done>Song and album pages expose the collapsible SVG visualization while preserving the table-first detail layout.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- src/components/detail/chart-run-visualization.tsx src/app/song/[id]/page.tsx src/app/album/[id]/page.tsx`
- [ ] `npm run build`
- [ ] Visualization appears only for 2+ data points and remains secondary to the history table
</verification>

<success_criteria>
- All tasks completed
- Song and album pages expose the required collapsible SVG visualization
- The visualization uses the correct tick sets, inverted rank scale, and peak annotation
- No new client fetch-on-toggle path is introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages-visualizations/03-04-SUMMARY.md`
</output>
