---
phase: 03-detail-pages-visualizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/songs.ts
  - src/lib/albums.ts
  - src/lib/artists.ts
  - src/app/api/songs/[id]/route.ts
  - src/app/api/albums/[id]/route.ts
  - src/app/api/artists/[id]/route.ts
autonomous: true
requirements:
  - DETAILS-01
  - DETAILS-02
  - DETAILS-03
  - DETAILS-04
  - DETAILS-05
must_haves:
  truths:
    - Song, album, and artist routes have typed server-safe data contracts that mirror the existing Python services.
    - Song and album detail payloads expose metadata, stats, artists, and chronological chart-run points.
    - Artist detail payloads expose profile aggregates plus catalog rows for Hot 100 songs and Billboard 200 albums.
  artifacts:
    - src/lib/songs.ts
    - src/lib/albums.ts
    - src/lib/artists.ts
    - src/app/api/songs/[id]/route.ts
    - src/app/api/albums/[id]/route.ts
    - src/app/api/artists/[id]/route.ts
  key_links:
    - src/app/api/songs/[id]/route.ts delegates to src/lib/songs.ts instead of embedding SQL inline.
    - src/app/api/albums/[id]/route.ts delegates to src/lib/albums.ts instead of embedding SQL inline.
    - src/app/api/artists/[id]/route.ts delegates to src/lib/artists.ts instead of embedding SQL inline.
---

<objective>
Create the typed backend contracts for all Phase 3 detail pages.

Purpose: Lock the server-side song, album, and artist data shape before page components and visualization work depend on it.
Output: Shared TypeScript detail helpers plus validated internal detail APIs for songs, albums, and artists.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages-visualizations/03-RESEARCH.md
@src/lib/db.ts
@src/lib/charts.ts
@src/app/api/health/route.ts
@billboard_stats/services/song_service.py
@billboard_stats/services/album_service.py
@billboard_stats/services/artist_service.py
@billboard_stats/models/schemas.py

<interfaces>
From src/lib/charts.ts:
```ts
export type ChartType = "hot-100" | "billboard-200";

export function parseChartType(value: string | null | undefined): ChartType | null;
export function isValidISODate(value: string): boolean;
```

From billboard_stats/models/schemas.py:
```py
class SongStats(BaseModel):
    total_weeks: int
    peak_position: Optional[int]
    weeks_at_peak: int
    weeks_at_number_one: int
    debut_date: Optional[date]
    last_date: Optional[date]
    debut_position: Optional[int]

class ArtistStats(BaseModel):
    total_hot100_songs: int
    total_b200_albums: int
    total_hot100_weeks: int
    total_b200_weeks: int
    hot100_number_ones: int
    b200_number_ones: int
    best_hot100_peak: Optional[int]
    best_b200_peak: Optional[int]
    first_chart_date: Optional[date]
    latest_chart_date: Optional[date]
    max_simultaneous_hot100: int
```
</interfaces>
</context>

<threat_model>
<trust_boundaries>
- Route params from the browser cross into Next.js route handlers.
- Detail helpers cross into Neon PostgreSQL through `src/lib/db.ts`.
- API responses expose entity metadata and aggregate counts back to the browser.
</trust_boundaries>
<stride_register>
- threat_id: TM-03-01
  category: tampering
  component: detail route params
  disposition: mitigate
  mitigation_plan: validate `[id]` params as positive integers before calling any helper or database query.
- threat_id: TM-03-02
  category: information-disclosure
  component: detail API error responses
  disposition: mitigate
  mitigation_plan: return concise 400/404/500 JSON payloads without exposing SQL text, stack traces, or connection details.
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Port the song and album detail service contracts into TypeScript helpers</name>
  <files>src/lib/songs.ts, src/lib/albums.ts</files>
  <read_first>AGENTS.md, src/lib/db.ts, src/lib/charts.ts, billboard_stats/services/song_service.py, billboard_stats/services/album_service.py, billboard_stats/models/schemas.py, .planning/phases/03-detail-pages-visualizations/03-RESEARCH.md</read_first>
  <action>Create `src/lib/songs.ts` and `src/lib/albums.ts` as the TypeScript translations of the Python `get_song()`, `get_album()`, and `get_chart_run()` contracts. Export concrete interfaces named `DetailArtistLink`, `DetailChartRunPoint`, `SongDetailPayload`, and `AlbumDetailPayload`. `SongDetailPayload` must contain `song`, `stats`, `artists`, `chartRun`, and `chartType: "hot-100"`. `AlbumDetailPayload` must contain `album`, `stats`, `artists`, `chartRun`, and `chartType: "billboard-200"`. Query the base entity table, the matching stats table, the join table for artists ordered by role then name, and the chronological chart run ordered by `cw.chart_date ASC`. Preserve phantom-week filtering by reusing the same valid-week CTE strategy already established in `src/lib/charts.ts`. Return `null` when the song or album row does not exist. Convert date values to ISO `YYYY-MM-DD` strings in the returned payload so server pages can format them without carrying `Date` objects across boundaries.</action>
  <verify>npm run lint -- src/lib/songs.ts src/lib/albums.ts</verify>
  <acceptance_criteria>
    - src/lib/songs.ts contains `export interface SongDetailPayload`
    - src/lib/songs.ts contains `chartType: "hot-100"`
    - src/lib/albums.ts contains `export interface AlbumDetailPayload`
    - src/lib/albums.ts contains `chartType: "billboard-200"`
    - src/lib/songs.ts contains `chartRun`
    - src/lib/albums.ts contains `chartRun`
  </acceptance_criteria>
  <done>Song and album detail helpers expose one stable typed payload each, including chronological chart-run data for Phase 3 pages.</done>
</task>

<task type="auto">
  <name>Task 2: Port the artist detail contract and expose all three detail APIs</name>
  <files>src/lib/artists.ts, src/app/api/songs/[id]/route.ts, src/app/api/albums/[id]/route.ts, src/app/api/artists/[id]/route.ts</files>
  <read_first>AGENTS.md, src/app/api/health/route.ts, src/lib/db.ts, src/lib/songs.ts, src/lib/albums.ts, billboard_stats/services/artist_service.py, billboard_stats/models/schemas.py, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md</read_first>
  <action>Create `src/lib/artists.ts` exporting `ArtistCatalogSongRow`, `ArtistCatalogAlbumRow`, and `ArtistDetailPayload`, plus a `getArtistDetail(artistId: number)` helper. The payload must include `artist`, `stats`, `songs`, and `albums`, where `songs` and `albums` are ordered by `debut_date` and carry the fields needed for the artist tables: `id`, `title`, `artist_credit`, `peak_position`, `total_weeks`, `weeks_at_peak`, `weeks_at_number_one`, `debut_date`, and `debut_position`. Then add `GET` handlers at `/api/songs/[id]`, `/api/albums/[id]`, and `/api/artists/[id]`. Each handler must parse `params.id`, reject non-positive integers with `400`, return `404` JSON when the helper returns `null`, and otherwise serialize the helper payload. Use the existing `health` route error-handling style for concise `500` responses. Keep all SQL inside the lib helpers; route files should only validate input and shape HTTP responses.</action>
  <verify>npm run lint -- src/lib/artists.ts src/app/api/songs/[id]/route.ts src/app/api/albums/[id]/route.ts src/app/api/artists/[id]/route.ts</verify>
  <acceptance_criteria>
    - src/lib/artists.ts contains `export interface ArtistDetailPayload`
    - src/lib/artists.ts contains `songs:`
    - src/lib/artists.ts contains `albums:`
    - src/app/api/songs/[id]/route.ts contains `status: 404`
    - src/app/api/albums/[id]/route.ts contains `status: 404`
    - src/app/api/artists/[id]/route.ts contains `status: 404`
  </acceptance_criteria>
  <done>All three detail entities have validated JSON endpoints backed by typed lib helpers instead of inline SQL.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- src/lib/songs.ts src/lib/albums.ts src/lib/artists.ts src/app/api/songs/[id]/route.ts src/app/api/albums/[id]/route.ts src/app/api/artists/[id]/route.ts`
- [ ] `npm run build`
- [ ] Song, album, and artist route handlers reject invalid IDs before calling DB helpers
</verification>

<success_criteria>
- All tasks completed
- TypeScript detail contracts mirror the Python service behavior and naming
- All detail routes have validated JSON handlers
- No route handler contains inline SQL
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages-visualizations/03-01-SUMMARY.md`
</output>
