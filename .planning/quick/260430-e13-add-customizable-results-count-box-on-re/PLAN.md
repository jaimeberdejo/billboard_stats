# Quick Task Plan

Add a customizable results-count box on the records page.

Steps:

1. Replace the passive top-right results label with a numeric input.
2. Accept values from 1 to 1000 in the UI.
3. Thread the requested limit through preset and custom records requests.
4. Remove the hardcoded custom-results slice so the backend respects the requested limit.
